# 更新日志

本文件的人工可读变更记录。新版本段落由 `node scripts/release.mjs <版本号>`
自动生成草稿（按提交类型归组），发布前请人工润色。

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

<!-- 新版本插入位置 -->

## [0.3.8] - 2026-09-20

### 修复

- 文本资源统一以磁盘文件为唯一事实来源取材，修复详情页编辑保存引发的系列不一致：编辑过的 .txt 资源卡片只显示 180 字摘要、外部编辑器改动文件后粘贴与详情页拿到过期内容（存在旧稿覆盖文件的数据丢失风险）、径向菜单预览面板内容过期且文件拖出失效；详情页编辑保存后记录归一为文件承载形态，存量降级记录由启动迁移自动修复

### 重构

- 移除无调用方的 update_clipboard_record 死命令及其源码回归断言（其重分类 type 的语义与文件承载原则相悖）


## [0.3.7] - 2026-09-20

### 功能

- 收藏状态直接由星标按钮本身表达：已收藏常显为灰色实心星（悬停不再变黑），未收藏仅在悬停卡片时显示描边星；移除卡片头部金色角标，径向菜单已收藏星同步改灰，主窗口星标补充 aria-pressed 状态标识

### 工程

- release 脚本兼容 CRLF 检出的 Cargo.lock，Windows 环境可直接执行发布


## [0.3.6] - 2026-09-19

### 功能

- 数据备份：设置页一键导出 / 导入整库压缩包（剪切板历史、快捷输入、设置，可选资源库）；逐表校验通过才切换，失败不碰现有数据
- 收藏：剪切板记录可收藏——星标操作、防保留期清理、列表恒定浮顶，主窗口与径向菜单均支持，并新增「收藏」筛选视图
- 资源回收站：应用内删除资源改为移入回收站，默认保留 30 天；支持单条恢复、彻底删除与一键清空，切换资源库后仍可恢复
- 查找替换：资源详情编辑模式与新建内容窗口支持 Ctrl+F 查找替换——匹配计数、上一个 / 下一个、替换与全部替换，Esc 关闭
- 双击编辑：资源详情页双击内容直接进入编辑，Ctrl+Enter 或双击空白处保存，双击文本内仍为选词

### 修复

- 备份导入先完成全部校验再落位资源库，校验失败不再覆盖现有数据；未随包导出资源库时保留本机库位置；导出经临时文件落位，中途失败不再损坏目标路径已有备份
- 删除资源入回收站后不再出现指向回收站的重复卡片；存量重复卡片会在升级后首次启动自动清理
- 切换资源库后仍能从旧库根恢复回收站文件；存储迁移完整保留回收站条目；保留期极端取值不再导致清理异常
- 编辑模式（详情页与新建窗口）打字时输入光标不再被查找功能抢占；查找条固定于内容区右上并随滚动常驻；Ctrl+F 可开 / 关查找条
- 径向菜单收藏星标与相邻动作按钮尺寸、焦点环、hover 反馈统一；星标按下不再误触发文件拖出
- 收藏 / 取消收藏跨窗口实时同步；并发刷新不再导致列表卡在加载态
- 回收站返回键与激活态样式、资源详情返回键样式与整体交互统一

### 其他

- 架构守卫新增 4 条规则（类别过滤唯一来源、动作按钮共享基类、卡片动作组覆盖检查等），防止同类问题复发
- 文档：新增备份 / 收藏 / 回收站实施计划与多轮复查结论记录

## [0.3.3] - 2026-09-15

### 修复

- 资源、剪切板与快捷输入的图片预览全部恢复：改经本地回环媒体服务加载，不再依赖已停用的 asset 协议
- 快捷输入的图片短语在编辑对话框中可正常预览：库外原始文件改走缩略图并纳入媒体服务放行
- 删除图片文件短语后不再残留缩略图缓存目录


## [0.3.2] - 2026-09-15

### 修复

- 深翻页新卡片不再隐身数十秒（入场动画延迟封顶），记录更新后深翻页不再回退第一页
- 短语「最近使用」排序不再受时间格式混排影响，改用数据库生成列排序
- 切换剪切板类别筛选不再丢失资源面板的分组浏览位置
- 径向菜单复用媒体预览时样式缺失，共享样式迁入公共分包
- 剪切板采集与资源监听线程异常后自动重建，后台捕获不再静默失效
- 存储迁移完整复制业务数据并搬迁附件目录，行数校验通过后才切换；迁移确认改用应用内对话框
- 启用 SQLite 外键约束，跨表引用完整性真正生效
- 资源库监听重建后补发防抖重扫，间隙丢事件可自愈
- API Key 预览按字符截断，非 ASCII 内容不再崩溃
- 设置装载失败不再被永久缓存，自动延迟重试
- 粘贴失败、文件夹选择失败与超时给出可见提示，不再静默吞掉
- 确认对话框支持 Esc 关闭与焦点管理，卡片键盘可达

### 性能

- 剪切板列表新增创建时间索引，深翻页与清理任务显著加速
- 资源列表改为数据库层过滤排序，页面组装更轻
- 整组粘贴/拖出只扫描目标分组子目录，未分组仅列库根一级文件
- Windows 剪贴板图片改用序列号判断变更，驻留期间不再反复全量拷贝
- 剪贴板缩略图直接从内存生成，采集路径不再整图解码
- 短语缩略图 LRU 缓存、隐藏面板暂停轮询、样式按窗口分包、logo 体积压缩约 15 倍

### 安全

- 媒体服务与图片读取限定在应用管理目录内，拒绝越界路径访问

### 重构

- 数据库层按业务域拆分为七个模块、快捷键层按窗口域拆分，命令注册与行为不变
- 普通/终端粘贴、视频封面帧等重复实现合并为单一执行路径
- 分组条滚轮横滚、拖拽排序与弹层 Esc 收敛为共享 hooks，各页交互一致

### 工程

- 发布链路补全：新增一键 release 脚本与 CHANGELOG，停用 asset 协议收紧 CSP，ctl 脚本随安装包分发
- CI 补 TypeScript 类型检查、Windows 编译检查，发布构建前置质量门禁
- 移除未使用依赖，pnpm 锁定为唯一包管理器

### 文档

- 修正 README 失实内容，补英文指南与 LICENSE
- 「快捷短语」命名统一为「快捷输入」

## [0.3.1] - 2026-08-29

### 修复

- 资源库变更事件丢失时列表 10 秒内自愈，补库修订号心跳对账
- 径向菜单扩展预览条带补齐窗口边界阴影
- 资源文件覆盖保存后预览不再停留旧图，媒体缓存按文件版本失效
- 资源库不再收录浏览器/系统临时文件，启动时清退历史误入库记录
- 主窗口切区/恢复显示后列表恒空，视图状态按页拆分根治

## [0.3.0] - 2026-08

### 修复

- 径向菜单 Linux 改常驻映射+屏幕外停泊，根除弹出/回收偏心动效
- 快捷键二次按压无法收起径向菜单（grab 抢焦点竞态）
- 焦点归还最后一道防线 + 全覆盖审查加固
- 常驻模型三处回归——弹窗可见判定、拖拽内容滞留、停泊竞态
- 主窗口切区/重开后的空列表，补自愈式刷新路径

### 重构

- 径向条带方向判定提取为纯函数并补 Rust 单测

## [0.2.0] - 2026-08

### 修复

- 外部删除/移入资源文件实时同步清理与去重（归一化路径键，兼容 Windows 大小写）
- 径向菜单剪切板条目的次数徽标与时间收进同一行
- 内容列表排序说明行补上缺失的弱化样式

### 测试

- 跨功能用户会话模拟与死命令清理
