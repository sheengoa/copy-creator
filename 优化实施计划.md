# Copy Creator 优化实施计划

- **关联文档**：[系统排查与优化方案.md](./系统排查与优化方案.md)（问题编号 A/B/P/E/F 沿用该文档，共 44 项）
- **分支策略**：实施在 `dev` 分支进行，每批完成后由项目所有者决定合入 `main` 的方式
- **本批范围**：阶段一（P0 止血）+ 阶段二（工程门禁），其余批次按本计划排期

---

## 执行原则

1. **顺序背后是依赖关系**：B1-a 的 `ensure_schema` 提取必须先于阶段三的 schema 变更（加列只改一处）；E2 的 Windows CI 必须先于阶段四的 shortcut/radial 拆分（双平台编译门禁先行）。
2. **提交纪律**：一个任务一个（或一组紧密关联的）提交，说明格式 `<类型>：<描述>`；每个提交前确保改动所涉及的四项验证通过——`pnpm typecheck`、`pnpm test`、`pnpm lint`、`cargo test`（E1 落地前 typecheck 以 `pnpm build` 的 tsc 步骤替代）。
3. **测试纪律（即日生效）**：`integrationRegression.test.ts` 冻结增量——新回归一律写成真实测试（组件行为 → jsdom + testing-library；数据路径 → Rust 内存 sqlite），不再向字符串守卫追加断言（E5 的前置约定）。
4. **手术式修改**：行为等价的重构（阶段四拆分类）单独成提交且不改任何行为，靠全量测试兜底。

---

## 阶段一：P0 止血（本批实施）

| # | 任务 | 提交 | 内容 | 验证方式 | 状态 |
|:--|:--|:--|:--|:--|:--|
| 0 | 落盘文档 | `文档：新增系统排查与优化方案与四阶段实施计划` | 两份文档入库 | — | ✅ |
| 1 | [B2] make_key_preview 字节切片 panic | `修复：API Key 预览按字符截断，非 ASCII 内容不再 panic` | `db.rs` 改 `chars()` 实现；新增 CJK / 混合 / 边界长度单测 | 新单测全绿；cargo test 全绿 | ✅ |
| 2 | [B3] 采集与监听线程 panic 隔离 | `修复：剪切板采集与资源监听线程增加 panic 隔离` | `clipboard.rs` monitor 循环体、`resource_watch.rs` watch 循环体包 `catch_unwind` + `log::error`，与 win_hook 既有做法对齐 | cargo test 全绿；人工验证采集正常 | ✅ |
| 3 | [B1-a] ensure_schema 单一来源 | `重构：统一建表与增量迁移为 ensure_schema 单一来源` | 从 `init_db` 提取建表 + ALTER 为 `ensure_schema(&Connection)`，`init_db` 与 `migrate_storage` 共用；消除 4 份 schema 拷贝（行为不变） | cargo test 全绿（73 项 db 测试兜底） | ✅ |
| 4 | [B1-b] 存储迁移完整复制 | `修复：存储迁移完整复制业务数据并搬迁附件目录` | `ATTACH` 旧库逐表 `INSERT SELECT`（7 张表）；存储目录内附件 / 快捷输入文件目录搬迁；行数校验通过后原子切换连接；补迁移集成测试 | 新迁移测试：迁移后各表行数与旧库一致、列表命令可用；人工走设置页迁移流程 | ✅ |

> 说明：原方案中的 `migratePrompt` 文案临时止损不再需要——完整修复与文案同批落地。

## 阶段二：工程门禁与杂项清理（本批实施）

| # | 任务 | 提交 | 内容 | 验证方式 | 状态 |
|:--|:--|:--|:--|:--|:--|
| 5 | [E3] 双锁文件 | `工程：移除过期 package-lock.json，锁定 pnpm 为唯一包管理器` | `git rm` package-lock.json；package.json 加 `packageManager` + `preinstall: only-allow pnpm`；两个 workflow 去掉 `version: 9`（由 packageManager 接管） | `pnpm install --frozen-lockfile` 可用；CI 绿 | ✅ |
| 6 | [E10] 生成器入口 | `工程：补充媒体类型生成与校验脚本入口` | package.json 加 `gen:media-types` / `gen:check` | `pnpm gen:check` 通过 | ✅ |
| 7 | [E6] 死依赖 | `工程：移除未使用的 reqwest 依赖，收窄 tokio features` | Cargo.toml 删 reqwest；tokio features 按实际使用收窄 | `cargo check` / `cargo test` 通过 | ✅ |
| 8 | [E1] 类型门禁 | `工程：CI 增加 TypeScript 类型检查步骤` | package.json 加 `typecheck` 脚本；ci.yml 插入步骤 | 本地 typecheck 通过；CI 绿 | ✅ |
| 9 | [E2] Windows 门禁 | `工程：CI 增加 Windows 编译与测试 job` | ci.yml 加 windows job（`cargo check` + `cargo test --lib`） | Windows job 首跑绿（先本地验证 cargo test --lib 可独立编译） | ✅ |
| 10 | [E4] 发布门禁 | `工程：发布构建前置测试门禁` | build.yml 加独立 gate job（lint + test + cargo test），build job `needs: gate`；加 `concurrency`；修正 ci.yml 失实注释 | workflow 语法检查；下次 tag 演练 | ✅ |
| 11 | [F2] 架构文档收尾 | `文档：完成架构方案文档删除并迁移规则出处` | 提交工作区已有的删除；4 处注释引用（architectureGuard / eslint / ci.yml / generate 脚本）改指 `src/domain/README.md`，必要时把规则原文沉淀进去 | `git grep "DOMAIN_ARCHITECTURE_PLAN"` 归零；`git grep "§5"` 归零 | ✅ |
| 12 | [F5] 本地产物 | `工程：清理本机专用脚本与过程产物入库` | dev.cmd 解除跟踪 + gitignore（保留本地文件）；删除 `.superpowers/` + gitignore；`src-tauri/test.cmd` 等通用脚本保留 | `git status` 干净；本地构建脚本仍可用 | ✅ |
| 13 | [F1] README 修正 | `文档：修正 README 失实内容并补 LICENSE` | logo 引用改 `logo_top.png`；补 LICENSE（含上游署名）；ctl 分发说明改为与打包配置一致；项目结构补 domain/hooks/config/scripts；EN 版同步；应用内 README 改为指回根 README | README 渲染检查；链接可达 | ✅ |

## 阶段三：性能与体验（本批实施）

| # | 任务 | 提交 | 关联问题 | 状态 |
|:--|:--|:--|:--|:--|
| 14a | `性能：剪切板新增 created_ms 生成列与索引，清理任务改 DELETE RETURNING，去重改毫秒比较` | `7399fab` | P3、B10（剪切板侧） | ✅ |
| 14b | `性能：资源列表只读标量行过滤排序，JSON 组装与文件 stat 仅对返回页执行` | `2c0263f` | P2 | ✅ |
| 15 | `性能：整组粘贴/拖出只扫描目标分组子目录，未分组仅列库根一级文件` | `d153836` | P4 | ✅ |
| 16 | `性能：Windows 剪贴板图片检测改用序列号判断变更，图片驻留期间不再每轮全量拷贝` | `b515a3f` | P1 | ✅ |
| 17a | `修复：卡片入场动画延迟封顶首屏` | `eb1b29b` | P5 | ✅ |
| 17b | `修复：记录更新事件改为重载当前已加载窗口` | `0560ee1` | P7 | ✅ |
| 17c | `性能：三页容器改选择器订阅，卡片 memo 化，径向菜单 items 计算与图片横幅订阅收窄` | `7eb08fe` | P6 | ✅ |
| 18a | `修复：剪切板页粘贴失败给出可见反馈，径向菜单粘贴失败留调试日志` | `5a46bed` | B4 | ✅ |
| 18b | `修复：domain 兜底文案改由调用方注入 locale，视图模型携带摘要字段` | `2f21545` | B5 | ✅ |
| 18c | `修复：设置装载失败不再永久缓存，清空缓存并安排延迟重试` | `a66803c` | B8 | ✅ |

> **#14b 方案微调说明**：原计划"resource_size/resource_modified 落库"。实施时发现 `resource_modified` 是媒体缓存失效键（`c32bae1` 的机制），落库后外部覆盖保存文件会导致缓存不失效（行为回归）。改为等价方案：查询只读标量行完成过滤/排序，JSON 组装与 stat 延迟到分页后仅对返回页（≤200 行）执行——消除全量 stat 与全量 JSON 构建两大开销，同时 100% 保留 stat 实时性与缓存失效语义。
>
> **#16 方案说明**：Windows 用 `GetClipboardSequenceNumber`（无锁、一次调用）精确判断剪贴板是否变过，未变化则跳过 `get_image()` 全量拷贝，检出延迟保持 800ms 不变；非 Windows 平台保持原行为。

## 阶段四：结构整固（远期批次，待排期）

| # | 任务 | 关联问题 | 备注 |
|:--|:--|:--|:--|
| 19 | db.rs 按域拆分（纯机械搬迁，行为不变） | A1 | 命令注册路径不变，118 项 Rust 测试兜底 |
| 20 | shortcut.rs 拆分 + RadialWindowController trait 化 | A2、A3 | 双平台径向回归：呼出 / 收起 / 停泊 / 拖出 |
| 21 | 径向预览并入 domain、eslint 清单补漏、"快捷短语"命名统一 | A4、F3 | `quick_input` 声明为历史等价名，新建接口不再使用 |
| 22 | 类型三处合一、store 改名拆分、重复逻辑合并（粘贴双胞胎 / 批量删除 / GroupChips / VideoPoster / 分页循环 / 分组态收敛） | A5-A9 | 纯行为等价变换 + 新抽纯函数单测 |
| 23 | 测试放置约定（`src/testing/`）+ 伪集成测试存量迁移 + ResourcePage 入口收敛 | E5、F4 | 先迁移高风险：resource_watch、粘贴路径 |
| 24 | watcher 重建补信号、键盘可达性 + dialog hook、媒体路径白名单 + ACAO 收紧、FK / IPC 硬化 | B6、B7、B9、B10 | 安全项含越界路径 404 测试 |
| 25 | 发布链路：asset scope 收窄、CSP 白名单、updater + 签名 + CHANGELOG、release 一键脚本、bundle.resources 补 ctl 脚本 | E7、E8、E9 | 需要一轮 tag 演练验证打包产物 |
| 26 | 打磨：图片编解码去重、短语缩略图缓存、隐藏面板停轮询、CSS 按窗口分包、components 目录化、logo 压缩、vite build 配置 | P8-P10、F6、E10 | — |

---

## 进度跟踪

- [x] 阶段一（#0-#4）——已完成（提交 `59528ae`…`c4365aa`）
- [x] 阶段二（#5-#13）——已完成（提交 `52e3629`…`5a47de2`）
- [x] 阶段三（#14-#18）——已完成（提交 `7399fab`…`a66803c`）
- [x] 阶段四（#19-#26）——**三批全部完成**（第一批 `2435f24`…`52f013e`，第二批 `cc97454`…`b4572e3`，第三批 `8ae7ff2` 起）；缓项见"阶段四第三批完成情况备注"（A3 完整 trait 化、updater+签名、P9 虚拟化）

每批完成后的动作：四项验证全绿 → 更新本文档进度与方案文档状态 → 由项目所有者验收后合入 `main`。

### 阶段四第一批完成情况备注（2026-09-15）

| 提交 | 内容 | 关联 |
|:--|:--|:--|
| `2435f24` | 存储迁移确认改用应用内 ConfirmDialog | 测试发现的缺陷：`window.confirm` 在 Tauri WebView 被禁用，「迁移」分支此前在 Windows 永不可达 |
| `bc7b2e6` | 资源库监听重建后无条件补发防抖重扫 | B6 |
| `db8c339` | 启用 SQLite 外键约束（主库 + 迁移新库） | B10 |
| `abcd16b` | 径向菜单文件短语预览改走 domain，eslint 出口清单补漏 | A4 |
| `30ffb34` | clipboardStore 删除手写类型副本，统一引用 types | A5 |
| `e38eaea` | 媒体服务与图片读取限定应用管理目录内 | B9（安全） |
| `84f3f3a`+`52f013e` | db 拆分目录模块，3352 行测试外迁至 `db/tests.rs` | #19 第一步 |

- 验证基线：`pnpm typecheck` 通过、vitest 141/141、ESLint 0 错误 0 警告、cargo test --lib 128/128（新增路径白名单 2 项测试）。
- B9 的白名单 = 存储目录 + 当前/默认/历史资源库目录；实现曾漏加存储目录（本机媒体服务测试当场拦截），已修复——测试有效性的直接例证。
- **GUI 整体回归（自动化 + 模拟人工）已通过**：中文 `sk-` 采集（B2）、资源新建/搜索（#14b/#17）、语言切换（B5）、粘贴焦点归还均验证正常；粘贴注入最后一步受 UWP 记事本对合成键盘事件的兼容性限制无法在本环境端到端验证（应用侧日志流程完整，注入代码未改动）。

### 阶段四第二批完成情况备注（2026-09-15）

| 提交 | 内容 | 关联 |
|:--|:--|:--|
| `cc97454` | 确认对话框 Esc + 焦点管理；剪贴板 / 短语卡片键盘可达（role/tabIndex/Enter-Space） | B7 |
| `1641424` | 普通 / 终端粘贴合并为统一 `runPaste` 执行路径 | A8 |
| `2180b10` | 短语粘贴合并为 `runPastePhrase`，乐观重排抽出 `bumpUsedPhrase` 纯函数 | A8 |
| `b33c844` | 剪贴板缩略图直接从内存 RGBA 缓冲生成，采集路径不再整图解码 PNG | P8 |
| `814d232` | 视频封面帧两份实现合并为共享 `components/VideoPoster.tsx`，守卫白名单同步 | A8 |
| `b4572e3` | 跨栈守卫测试集中到 `src/testing/`，附放置约定 README | #23 |

- 验证基线：`pnpm typecheck` 通过、vitest 141/141、ESLint 0 错误 0 警告、cargo test --lib 128/128。

### 阶段四第三批完成情况备注（2026-09-15，本批已全部实施）

| 提交 | 内容 | 关联 |
|:--|:--|:--|
| `8ae7ff2` | ResourcePage 弹层 Esc 关闭统一为共享 `useEscapeKey` hook | #24 余项 |
| `db84a8e` | 短语「最近使用」排序改走 `last_used_ms` 生成列，不再依赖 RFC3339 文本字典序 | B10 短语侧 |
| `87656bf` | 资源分组选中态收敛进 store 单一来源，页面只读写 store | A9 |
| `aa3c949` | 分组条滚轮横滚与拖拽排序收敛为共享 hooks，虚影统一 `ChipDragOverlay`；引入 jsdom + testing-library 组件测试链路 | #22（GroupChips 收敛） |
| `c45f402` | 短语缩略图 LRU 缓存、隐藏面板停轮询对账、CSS 按窗口分包（共享 34KB + 径向 16.6KB + 对话框 22.9KB + 主窗口 53.2KB）、logo 462KB→31KB | P10、F6 |
| `adb6cf3` | 「快捷短语」并行名统一为「快捷输入」，README 声明 `quick_input`/`phrases` 为等价历史标识 | F3 |
| `4c9968f` | db 模块按业务域拆为七子文件（mod 1305 行 + clipboard/resource/phrase/media/settings/migrate/apikeys），命令注册路径与行为不变 | #19 余项 |
| `26ec276` | shortcut 按窗口域拆为四文件（shortcut/radial_window/clipboard_create_window/win_hook）；`RADIAL_MENU_SHOWN`/`_ENABLED` 读写收敛到 radial_window 单一入口 | #20（A2+A3） |
| `ca959d4` | 资源监听的事件分类与跨 tick 聚合抽出纯实现，补 6 项单测 | #23 余项 |
| `9c911e4`+`816d23a` | 停用 asset 协议并移除 `protocol-asset` feature、CSP 去掉 `https:` 外联与 asset 源、targets 显式四件套、ctl 脚本随包、新增 `scripts/release.mjs`（四处版本号同步 + CHANGELOG 草稿 + tag）、落盘 CHANGELOG.md | E7、E8、E9、F1 收尾 |

- 验证基线：`pnpm typecheck` 通过、vitest 144/144、ESLint 0 错误 0 警告、cargo check 0 警告、cargo test --lib 135/135。
- **A3 完整 trait 化（RadialWindowController）记录为缓项**：标志位翻转已收敛单一入口（审计认可的最低线）；完整 trait 抽象需 Windows + Linux 双平台径向行为回归（呼出/收起/停泊/拖出），本环境只能验证 Windows 侧，留待有双平台回归条件时实施。Linux 侧编译由 CI ubuntu job 兜底验证（推送后确认首跑）。
- **updater + 签名（E8 中期项）需外部条件**：`TAURI_SIGNING_PRIVATE_KEY` 签名密钥与更新通道地址需项目所有者提供，接入 `tauri-plugin-updater` 后在 release 脚本补充签名步骤。
- P9 列表虚拟化维持显式缓项（已有 content-visibility/懒加载缓解，见阶段三备注）。
- 发布演练（tag 触发 build.yml）待下次真实发布执行；届时确认 Windows job 首跑与四件套产物。

### 阶段四第二批（原"待排期"清单，大部分已随第二批落地）

- #19 余项：db/mod.rs（5692 行）按业务域继续拆分为 `db/clipboard.rs`、`db/resource.rs`、`db/phrase.rs` 等（跨域私有助手需逐个调整可见性）
- #20：shortcut.rs（1872 行）拆分 + RadialWindowController trait 化（双平台径向回归）
- #21 余项 / F3："快捷短语"命名统一（4 套并行名收敛）
- #22 余项：pasteRecord 双胞胎合并、GroupChips 收敛、VideoPoster 提共享、资源分组态收敛
- #23：测试放置约定（`src/testing/`）+ 伪集成测试存量迁移
- #24 余项：B7 键盘可达性（卡片 tabIndex、对话框 Esc 统一 hook）
- #25：发布链路（asset scope 收窄、CSP 白名单、updater + 签名、CHANGELOG、release 脚本、bundle.resources 补 ctl 脚本）
- #26：打磨（P8 图片编解码去重、P10 缓存/分包、F6、E10 余项）

### 阶段三完成情况备注（2026-09-15）

- 验证基线：`pnpm typecheck` 通过、vitest 141/141、ESLint 0 错误 0 警告、cargo test --lib 126/126（新增 created_ms 生成列、清理语义、子树扫描共 4 项测试）。
- 本机 `cargo test`（含 bin 目标）因 manifest 注入方式无法只作用于 lib 测试而链接失败——这是 `test.cmd` 注释中记录的既有本机限制，非本次改动引入；CI 的 Linux job 跑完整 `cargo test`，Windows job 用 `test.cmd`（--lib），均不受影响。
- 前端渲染纪律的量化预期：hover/搜索等高频操作不再触发整页卡片级重渲（径向菜单 items memo 化、三页容器选择器订阅、卡片 memo）；图片驻留期间的剪贴板轮询开销（Windows）从每 800ms 全量拷贝降为一次序列号读取。建议合入前在真实数据规模下人工感受资源库切换与径向菜单打开速度。
- B10 的短语侧（`last_used_at` 字符串排序）未在本批处理，维持现状；如需彻底统一可在阶段四随 phraseStore 改造一并处理。

### 阶段一、二完成情况备注（2026-09-15）

- 验证基线：`pnpm typecheck` 通过、vitest 141/141、ESLint 0 错误、cargo test 123/123（含 B1 迁移新增 2 项、B2 预览新增 4 项测试）。
- 顺带修复两项基线外问题：资源扫描测试的临时文件排除期望未随 `b66122d` 同步（main 上即已失败）；迁移 schema 守卫断言对齐 `ensure_schema` 新结构。
- #9 的 Windows CI job 语法已就绪但**尚未在真实 runner 上运行过**——推送 dev 分支或开 PR 后需确认首跑绿；本机已用等效方式（cargo test --lib + manifest 注入，即 test.cmd 同款）验证通过。
- 合入 `main` 前建议人工验收：设置页走一遍存储迁移流程（确认业务数据与图片完整可用）；复制一条中文 `sk-` 前缀文本验证采集正常、列表可打开。

## 双平台回归清单（阶段四前后适用）

| 平台 | 重点项 |
|:--|:--|
| Windows | 径向菜单呼出 / 收起 / 快捷键二次按压、OLE 文件拖出、粘贴焦点归还、自启动 |
| Linux（Wayland/X11） | 系统快捷键 + `copy-creator-ctl`、径向常驻停泊、AppImage 启动、剪贴板图文采集 |
