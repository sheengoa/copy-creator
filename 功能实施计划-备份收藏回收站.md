# 新功能实施计划：数据备份、收藏、资源回收站

> 状态：已确认，实施中（2026-09-18 决策点全部确定）
> 基线：dev 分支 b206cd8（v0.3.5）
> 定位前提：轻量、本地优先的 PC 复用效率工具，主链路「复制 → 存 → 找 → 用」。
> 本批三个功能共同补齐「存得住、丢不了」这一环。

---

## 一、背景与目标

使用两天的用户反馈没有遇到 bug，说明现有链路稳定。当前最薄的一环是**数据安全**：

1. 剪切板历史、快捷输入、分组、备注全部存于单个 SQLite 库 + 附件目录，**没有任何备份导出手段**，磁盘故障或换机即全丢；
2. 剪切板记录受保留期清理（`clipboard_retention`），重要内容只能手动转存资源库自救，**没有"收藏"保护**；
3. 资源库文件在应用内删除即 `std::fs::remove_file`，**误删不可逆**。

三个功能分别对应：保命（备份）、闭环（收藏）、后悔药（回收站）。

---

## 二、代码基础调研结论

以下为实施前摸底的结论（文件位置均相对 `copy-creator/src-tauri/src/` 与 `copy-creator/src/`）：

### 可直接复用的基建

| 基建 | 位置 | 对本批功能的意义 |
|:---|:---|:---|
| 存储切换/迁移 | `db/migrate.rs`（`migrate_storage`、`migrate_storage_data`、`copy_storage_dir_tree`） | 已有"复制业务数据 + 附件目录 + 行数校验 + 重启生效"完整先例，备份导入沿同一 UX（重启提示 `StorageSection.tsx:72-82` 已存在） |
| 保留期清理 | `db/clipboard.rs:71-171` `prune_old_records`，启动 + 每小时调用（`lib.rs:178,226-229`） | 收藏排除、回收站过期清空都挂同一任务 |
| 删除收敛点 | `delete_clipboard_records_internal`（`db/clipboard.rs:736-859`），资源文件删除走 `remove_resource_record_files`（`db/mod.rs:873-924`）、`delete_external_resource_file`（`db/clipboard.rs:663-668`） | 回收站只需在这几处把"直接删"改为"移入 .trash"，无需新开删除路径 |
| 隐藏目录免疫 | 资源库遍历跳过点前缀目录（`db/resource.rs:1080`，`.copy-creator` 同款机制） | `<库根>/.trash` 天然不会被资源发现误收录，监听侧零改动 |
| 附件目录清单 | 存储目录下 `images/`（含 `thumbs/`）、`stash-images/`、`quick-input-files/`（`db/migrate.rs:151` 整树搬迁） | 备份导出的文件清单与之完全一致 |
| schema 惯例 | `ensure_schema`（`db/mod.rs:949-1269`）：幂等建表 + `ALTER TABLE ... .ok()` 增量列 | 新增列/新表照此模式，无版本机制负担 |

### 两个必须区分的现状

- **现有「移到顶部」≠ 收藏**：卡片 hover 的 ↑ 按钮、右键置顶项和批量置顶只是把 `sort_order` 写成当前时间戳（`db/clipboard.rs:872` `move_clipboard_records_to_top`），**不防清理**。收藏是新能力（防删 + 浮顶），与本功能重合度高，按"整合不重复"原则由收藏**取代**该手势，详见第四节。
- **资源删除复用剪切板删除命令**：应用内删资源没有独立命令，走 `invoke("delete_clipboard_records")` 按 `storage_mode='resource'` / `resource-file:` id 分流（`pages/ResourcePage.tsx:657-669` → `stores/clipboardStore.ts:576-581`）。回收站改造落在后端删除收敛点即可覆盖单删与批删。

---

## 三、同类项目参照

- [EcoPaste](https://github.com/EcoPasteHub/EcoPaste)（同为 Tauri，最直接竞品）：有**收藏内容**入口与**存储备份设置**（[知乎评测](https://zhuanlan.zhihu.com/)、[博客园](https://www.cnblogs.com/)）；搜索用 SQLite FTS5。印证"收藏 + 备份"是该品类用户预期内的功能。
- CopyQ：Export/Import 以标签页为单位打包，可选加密——验证了"整库打包 + 导入前概要确认"的交互惯例。
- Ditto：`.dto` 专有格式导出；sticky 置顶条目与保留策略解耦——与本项目"收藏防清理"思路一致。

结论：三个功能均有成熟先例，交互可循惯例，无需发明新模式。

---

## 四、功能 A：收藏（防清理置顶）

> 排在第一个实施：改动最小、无新依赖，且为回收站 warm up 清理任务的改动面。

### 需求

- 剪切板记录可"收藏"，收藏记录**不受保留期清理影响**，且恒定**浮在列表最上方**；
- **整合而非并存**（避免功能重合、重复造轮子）：现有「移到顶部」手势（卡片 hover 的 ↑ 按钮、右键菜单置顶项、批量选择置顶，均为把 `sort_order` 写成当前时间戳的一次性挪顶，`move_clipboard_records_to_top`）**被收藏取代**——收藏完整覆盖其"挪到顶部"的诉求且更持久。快捷输入页短语自身的置顶（`move_phrases_to_top`）是独立页面的独立功能，不在整合范围。

### 技术方案

1. **DB**：`ensure_schema` 中 `ALTER TABLE clipboard_records ADD COLUMN pinned INTEGER NOT NULL DEFAULT 0`（幂等惯例）；新增命令 `set_clipboard_record_pinned(id, pinned)`，支持单条与批量。
2. **清理排除**：`prune_old_records` 的 `DELETE ... RETURNING` 加 `AND pinned = 0`（`db/clipboard.rs:71-171`），连带其按内容哈希的图片文件删除逻辑自动不触发。
3. **排序（单点生效，杜绝界面分叉）**：剪切板记录全项目只有 `get_clipboard_records` 一条查询通路（主窗口、径向菜单、新建窗口共用 clipboardStore），在 `db/clipboard.rs:187-193` 的三类 `content_sort` 排序分支统一前置 `pinned DESC`，三个界面自动一致，不存在"A 界面有 B 界面没有"的分叉面。
4. **RecordView**：`domain/recordView.ts` 增加 `pinned` 事实字段（透传，符合守卫规则 6"组装-only"）；`ClipboardCard` 只做渲染分支（星标角标），判定不进叶子。
5. **UI**（实现前按约定先出可视化小样确认）：
   - 卡片 hover 星标按钮 + 右键菜单「收藏 / 取消收藏」，**替换**现有 ↑ 移到顶部按钮与置顶菜单项；批量选择栏「置顶」同步替换为「收藏」；
   - 剪切板页类别筛选旁新增「收藏」筛选 chip；径向菜单条带不加收藏标记（共用查询自然浮顶，避免高密度布局再加视觉负担）；
   - 手动删除允许作用于收藏记录（用户主动删除优先于收藏保护）。

### 行为变化声明（整合代价）

- 移除剪切板页「移到顶部」手势后，"临时把某条挪到顶部"需改用收藏（更持久）；默认「最近使用」排序下，粘贴本身就会把记录顶起，实际影响很小。被取代的 `move_clipboard_records_to_top` 命令在 UI 无使用点后一并移除，避免死代码。

### 范围界定

仅剪切板记录。资源与快捷输入不受保留期清理，无此痛点，不扩（后续有需再议）。

### 验收

- `cargo test`：prune 排除 pinned 的断言、三类排序分支的 `pinned DESC` 断言；
- `pnpm test`：recordView 新字段、守卫测试全绿；
- `pnpm lint`：确认无残留的 `move_clipboard_records_to_top` 引用；
- 人工：收藏含图记录 → 保留期调到 1 周 → 触发清理 → 记录与图片存活；主窗口/径向菜单两处收藏记录均浮顶；取消收藏后按期清理。

---

## 五、功能 B：数据备份导出 / 导入

### 需求

- 一键把**业务数据整库**导出为一个 zip 文件：SQLite 库 + 全部附件（剪切板图片、暂存图文、快捷输入文件副本）+ 设置；
- 导入 zip 后**重启生效**，行数校验通过才切换，失败不碰现有数据；
- 资源库目录为可选项（体积可能巨大），勾选时一并打包。

### 导出格式

```
copy-creator-backup-<yyyymmdd-hhmmss>.zip
├── manifest.json          # format_version、app_version、exported_at、各业务表行数、资源库原始路径
├── data.db                # VACUUM INTO 生成的紧凑一致快照（免 WAL 处理）
├── images/                # ┐
├── stash-images/          # │ 存储目录整树，唯二例外见下
├── quick-input-files/     # ┘
└── library/               # 可选：资源库整树
```

**排除项（已逐一核实可安全再生）**：

- `images/thumbs/`、`quick-input-files/*/thumbs/`：缩略图缓存，`get_image_thumbnail` 缺失时回退全图现场生成（`db/media.rs:77-80` "Fallback: generate thumbnail from full image"）；
- `video-posters/`：视频海报缓存，前端在缓存缺失时自动回退现场抽帧（`db/media.rs:146-147` 注释明确"调用方回退现场抽帧流程"）；
- 排除规则实现为"存储目录整树 − 再生缓存黑名单"（而非白名单枚举目录），未来新增缓存目录不会静默漏备或错备。

### 技术方案

1. **依赖**：Rust 侧新增 `zip` crate（当前 Cargo.toml 无此依赖，是唯一新增依赖；rusqlite 0.31 bundled 的 SQLite 版本支持 `VACUUM INTO`）。
2. **导出命令** `export_backup(target_zip, include_library)`：
   - `VACUUM INTO` 生成 data.db 快照 → 拷贝存储目录整树（应用上述排除规则）→ 可选拷贝资源库树 → 收集各表 COUNT 写 manifest → 打包；
   - 前端经 `tauri_plugin_dialog` 选保存路径，导出中显示进度、可取消（大库场景）。
3. **导入命令** `preview_backup(zip)` + `import_backup(zip, restore_library_to)`：
   - 预览：读 manifest 返回概要（应用版本、各表行数、是否含资源库），供确认对话框展示；
   - 导入：解压到 `app_data_dir/restore-<ts>/` → 打开暂存库做**逐表行数校验 + `PRAGMA foreign_key_check`**（对齐 migrate.rs 的校验强度）→ 用户确认"覆盖当前数据" → `set_setting_skip_migrate("storage_path", ...)` 指向暂存目录 → 提示重启；旧存储目录保留不删（沿用迁移惯例）；
   - 重启后 `db_path` 链式跟随（`db/mod.rs:234-279`）自动切到新库。
4. **设置与路径语义**：
   - 设置随库整体恢复，`storage_path` 由导入流程覆写；快捷键设置恢复后重启自动重新注册（与 migrate 清空快捷键的行为不同，作为决策点②确认）；
   - `resource_library_path`：zip 含 `library/` 时由用户选恢复位置（默认沿用原路径），恢复后写回设置并触发 `sync_resource_library` 全量对账；**不含时若原路径在新机器不存在，确认对话框必须明示并引导重选**，不得依赖"路径不存在自动建空目录"的现有回退静默产出空库。

### 已知风险与对策

- **导入确认到重启之间的窗口期**：新复制的记录写入旧库，重启后丢失。对策：确认对话框明示"导入后请立即重启"，重启提示常驻直到执行。

### UI

`components/settings/StorageSection.tsx` 新增"备份"区块：导出按钮（选位置）、导入按钮（选 zip → 概要确认 → 重启提示），复用该组件既有的 ConfirmDialog / 重启提示样式——备份入口与既有存储迁移入口同区，用户对"改存储路径要重启"的心智直接复用，不另起一套交互。

### 验收

- Round-trip：导出 → 删数据造乱 → 导入 → 重启 → 逐表行数与导出前一致、图片可预览且缩略图自动再生、快捷输入文件副本可粘贴、视频海报缺失时自动抽帧、资源库（含备注/分组）还原；
- 校验失败路径：手工截断 zip → 导入报错且现有数据无损；
- 旧版本数据（如缺 pinned 列的旧库）导入后 `ensure_schema` 自动补列。

---

## 六、功能 C：资源回收站

### 需求

- 应用内删除资源文件（单个/批量）不再物理删除，移入库根 `.trash/` 目录；
- 回收站内可查看（文件名、原分组、删除时间）、**恢复**（回原分组，冲突自动改名）、**彻底删除**、**清空**；
- 保留 30 天（默认，settings 键 `trash_retention` 预留），随保留期清理任务自动过期清空；
- 外部删除（文件管理器直接删）维持现状——文件已不在磁盘，无法回收，感知清理逻辑不改。

### 技术方案

1. **新表** `trash_items`：`id PK, record_json TEXT, original_group, original_path, file_name, trashed_at`。删除资源时把 `clipboard_records` 整行序列化存入 `record_json`（恢复时原样 INSERT 回，保住分组/备注/类型等全部元数据）。
2. **删除路径改造**（三处收敛点）：`remove_resource_record_files`（`db/mod.rs:873-924`）与 `delete_external_resource_file`（`db/clipboard.rs:663-668`）改为"先写 `trash_items`，再把文件 move 到 `<库根>/.trash/<trashed_at>-<uuid>/`"；托管附件/缩略图随文件同目录一并移入。
3. **与文件监听的竞态（已核实机制，时序是硬要求）**：watcher 事件经 500ms 防抖后由 `settle_external_resource_changes`（`db/resource.rs:482-505`）统一裁决，其中 `relocate_resource_records`（目录改名重定向）与 `forget_resource_records`（`resource.rs:176-212`，"文件不在、记录不留"）都只作用于 `clipboard_records` 里的现存行。因此**命令内必须同步完成"先写 `trash_items` 行（事务内）、后移文件、移动失败回滚事务"**——settle 运行时记录行已不在 `clipboard_records`，重定向与清退均无对象，不会被误判为目录改名跟随进 `.trash`，也不会产生悬空记录。
4. **恢复/清空命令**：`list_trash_items` / `restore_trash_item(id)`（文件 move 回原分组目录，同名加序号后缀；`trash_items` 行转回 `clipboard_records` 行）/ `purge_trash_items(ids | all)`（物理删除）。恢复同样是"先插行、后移文件"的同步操作：恢复出的文件会触发 watcher 的到达事件，`discover_external_resource_files`（`resource.rs:218`）按路径键查 `by_path` 去重（`resource.rs:239-262`），插行在前则自动发现不会重复建记录；防御性兜底：恢复命令末尾按 `resource_path` 清理可能已存在的幽灵记录（极端时序下自动发现先建的无元数据记录）。
5. **过期清理**：`prune_old_records` 同任务追加"删 `trashed_at` 超期的 `trash_items` 行 + 对应 `.trash` 文件"。
6. **UI**（实现前按约定先出可视化方案确认）：ResourcePage 工具区新增「回收站」入口，打开回收站列表视图（挂既有 `resource-groups-changed` 刷新事件）；条目操作：恢复 / 彻底删除，头部：清空。

### 范围界定

- **v1 只覆盖资源库文件**：它是用户沉淀的内容资产。剪切板图片/文本沿用保留期模型（本身即软删除语义），快捷输入只删应用内副本、原件在用户手里，均不进回收站。
- `.trash` 为点前缀目录，资源发现（`resource.rs:1080`）与径向整组操作自动免疫，无需额外过滤。

### 验收

- `cargo test`：删除→trash_items 入库且文件位于 .trash；恢复→记录字段完整、分组正确、同名冲突改名、同路径幽灵记录被清理；过期清空；移动失败事务回滚；
- `pnpm test`：回收站视图为独立轻量列表（`trash_items` 行已离开 `clipboard_records`，不构成 `RecordView`，卡片叶子合同不适用），不引入对 stores 的越界引用；刷新挂 `resource-groups-changed` 既有事件；
- `pnpm lint` 全绿；
- 人工：删除 → 文件管理器可见 `.trash` 内容（"内容即文件"哲学下回收站同样透明）→ 恢复 → 缩略图/预览正常；删除/恢复后资源页、径向菜单资源条带、分组计数三处一致；外部删除路径回归不受影响。

---

## 七、实施顺序与提交拆分

按风险递增排序，每个阶段独立可发布、独立回滚：

| 阶段 | 内容 | 预估 | 验证方式 | 提交 |
|:---|:---|:---|:---|:---|
| 1 | 收藏：DB 列 → 命令 → prune 排除 → 排序 → RecordView → UI（含可视化小样确认），并整合移除「移到顶部」手势与 `move_clipboard_records_to_top` | 小 | `cargo test` + `pnpm test` + `pnpm lint` + 人工清理场景 + 主窗口/径向菜单浮顶一致性 | `功能：剪切板记录收藏，防保留期清理并浮顶，整合移除移到顶部` |
| 2 | 备份：zip 依赖 → 导出/预览/导入命令 → StorageSection UI → round-trip 验证 | 中 | 同上 + 导入失败不伤数据场景 | `功能：设置页新增数据备份导出与导入` |
| 3 | 回收站：trash_items 表 → 删除路径改造 → 恢复/清空命令 → 资源页入口 UI（含可视化方案确认） | 中偏大 | 同上 + watcher 竞态测试 | 按内部步骤拆 2-3 个提交（数据层 / 命令 / UI） |

三个功能完成合并为 v0.4.0 发布（`scripts/release.mjs` 走既有发布链路）。

**全批共同约束**（来自项目架构约定）：

- RecordView 只加事实字段透传，判定逻辑进 domain；叶子组件不 import stores（守卫规则 6/10）；
- 回收站等新增视图刷新必须挂 `resource-groups-changed` 既有事件；
- 每阶段全绿（`pnpm test`、`pnpm lint`、`cargo test`）才提交；提交前 `git diff --check` 与 `git diff --cached --check`。

---

## 八、决策点

### 已定（按"整合不重复"原则确定）

1. **收藏取代剪切板页「移到顶部」**：hover ↑ 按钮、右键置顶项、批量置顶全部替换为收藏，`move_clipboard_records_to_top` 随之移除；快捷输入页短语置顶不动。详见第四节"行为变化声明"。
2. **再生缓存不随备份导出**：`thumbs/`、`video-posters/` 均已核实具备自动再生能力（缩略图回退全图生成、海报回退现场抽帧），按"整树 − 缓存黑名单"排除。详见第五节。

### 已定（2026-09-18 用户确认：全部按建议执行）

3. **导入时快捷键设置**：随库恢复、重启自动重注册。
4. **备份默认不勾选资源库**：界面说明"资源库即普通文件夹，可用任意文件工具备份"，勾选时显示体积预估。
5. **回收站保留期**：默认 30 天，v1 为常量 + 预留 settings 键（`trash_retention`），设置页暴露放后续版本。
6. **UI 形态**（回收站入口位置与列表样式、收藏星标样式、批量栏动作措辞）：按设计讨论约定，每阶段 UI 实施前出可视化原型单独确认。

## 附：本批不做

- 截图直存（快捷键截图进剪切板历史/资源库）：经评估明确不做，不列入后续规划；
- 剪切板图片/文本进回收站（保留期模型即软删除）；
- 外部删除的回收（文件已不在磁盘；如未来需求强烈，可评估改接系统回收站 `trash` crate，跨 Win/Linux 一致）；
- 备份加密、增量备份、自动定时备份（列入远期候选）。
