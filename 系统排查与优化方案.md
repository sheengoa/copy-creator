# Copy Creator 系统排查与优化方案

- **排查日期**：2026-09-14
- **排查基线**：commit `59f86a8`（v0.3.1）
- **范围**：前端 `copy-creator/src/`（87 个 TS/TSX，约 1.75 万行）、后端 `copy-creator/src-tauri/src/`（13 个 .rs，约 1.65 万行）、构建与 CI 配置、仓库组织
- **方法**：按"后端实现 / 前端实现 / 工程化 / 仓库组织"四个专项并行做只读代码审查，关键结论由执行者逐条抽查原始代码复核（复核清单见附录）。静态审查为主，未运行构建与完整测试套件。
- **严重度定义**：P0 = 数据损坏 / 崩溃级缺陷；P1 = 显著正确性 / 性能 / 门禁缺口；P2 = 架构债务与可维护性；P3 = 低风险打磨项。

> 注：排查进行期间工作区发生了一次变更——`DOMAIN_ARCHITECTURE_PLAN.md` 从工作区被删除（git 仍在跟踪，删除未暂存），根目录《优化简要.md》也被移除。本文档以删除后的当前状态为准，相关条目（F2）按"待决断"处理。

---

## 一、总体结论

项目的工程基础是扎实的：领域层（`src/domain/`）真实落地且守卫有效、扩展名清单单一源生成链路完整、生产路径错误处理纪律好（db.rs 429 个 unwrap 中 428 个在测试模块）、Rust 侧有 118 个真实行为测试。**但本次排查发现 2 个 P0 级业务缺陷**（存储迁移静默丢弃用户数据、API Key 预览字节切片可致采集停摆与数据层锁死），以及三类系统性问题：

1. **性能热点集中在数据访问层**：剪贴板图片轮询、资源列表全量加载、周期清理任务在大数据量下都会造成可感卡顿，且都发生在全局数据库锁内；
2. **工程门禁有洞**：TypeScript 未开 strict、CI 不做类型检查、Windows 代码只在发版日才编译、发版不跑测试；
3. **两处巨石与命名分裂**：db.rs（8510 行、70 个命令、4 份 schema 拷贝）与"快捷短语"概念的 4 套命名，是当前最大的长期维护成本来源。

问题总数：**P0 × 2，P1 × 13，P2 × 19，P3 × 10**（详见第二节总览表）。

---

## 二、问题总览

| 编号 | 级别 | 维度 | 问题 | 主要位置 |
|:--|:--|:--|:--|:--|
| B1 | P0 | 业务 | 存储"迁移"不复制任何业务数据，与 UI 承诺相反；新库 schema 缺 4 列 | `db.rs:3717-3840` |
| B2 | P0 | 业务 | `make_key_preview` 按字节切片，非 ASCII 内容 panic，可致采集停摆 / 数据层锁死 | `db.rs:57-65` |
| B3 | P1 | 业务 | 采集 / 资源监听线程无 panic 隔离，线程死亡即静默停采 | `clipboard.rs:1110` |
| B4 | P1 | 业务 | 剪切板页与径向菜单粘贴失败被静默吞掉，无用户反馈 | `ClipboardPage/index.tsx:91` |
| B5 | P1 | 业务 | domain 层硬编码中文兜底文案，英文界面漏出 | `domain/records.ts:76,98` |
| P1 | P1 | 性能 | 剪贴板 800ms 轮询无条件全量读取图片（4K 图 ≈33MB/次） | `clipboard.rs:1113-1135` |
| P2 | P1 | 性能 | 资源列表 SQL 无分页全量加载 + 逐行 stat + 内存过滤排序，锁内执行 | `db.rs:1997-2187` |
| P3 | P1 | 性能 | 每小时清理任务非 sargable 条件 + 无索引 content 逐条 COUNT | `db.rs:1637-1680` |
| P4 | P1 | 性能 | 整组粘贴 / 拖出触发整个资源库递归扫描 | `db.rs:2624` |
| P5 | P1 | 性能 | 卡片入场动画按全列表 index 线性延迟，深翻页卡片"隐身"数十秒 | `ClipboardCard.tsx:243` |
| P6 | P1 | 性能 | 三个列表容器全量 store 订阅 + 卡片零 memo + 径向菜单 items 无 memo | `ResourcePage.tsx:70` 等 |
| E1 | P1 | 工程化 | TypeScript 全项目未开 strict；CI 不跑类型检查 | `tsconfig.app.json`、`ci.yml` |
| E2 | P1 | 工程化 | CI 无 Windows job，Windows 专属代码只在发版时编译 | `ci.yml:13` |
| E3 | P1 | 工程化 | 双 lockfile 并存，package-lock.json 停在 v0.1.13 仍被跟踪 | `copy-creator/package-lock.json` |
| F1 | P1 | 结构 | README 三处失实（logo 裂图 / LICENSE 缺失 / ctl 脚本分发描述）+ 应用内 README 过时 | `README.md:9,117,225` |
| F2 | P1 | 结构 | DOMAIN_ARCHITECTURE_PLAN.md 处于"半删除"状态，且有 4 处代码注释悬空引用 | 仓库根 / `eslint.config.js:23` 等 |
| F3 | P1 | 结构 | "快捷短语"概念存在 4 套并行命名（phrase / quick_input / 快捷输入 / 快捷短语） | 全仓 |
| P7 | P2 | 性能 | 记录更新事件触发整列表重载回第一页，深翻页状态丢失 | `clipboardStore.ts:288` |
| P8 | P2 | 性能 | 图片链路重复编解码 + 33MB 级像素克隆 | `clipboard.rs:1199`、`paste.rs:996` |
| A1 | P2 | 架构 | db.rs 巨石：70 命令 / 7 表 / 6 业务域 / 4 份 schema 拷贝 / 单连接全局串行 | `db.rs` |
| A2 | P2 | 架构 | shortcut.rs 混装 4 类职责（热键 / 径向窗口 / Win32 钩子 / 新建窗口几何） | `shortcut.rs` |
| A3 | P2 | 架构 | 双平台窗口模型靠标志位 + cfg 交错维系，缺统一抽象 | `radial_drag.rs` 等 |
| A4 | P2 | 架构 | RadialMenu 绕过 domain 出口直接 invoke，预览判定与 domain/preview.ts 平行实现 | `RadialMenu/index.tsx:504` |
| A5 | P2 | 架构 | ClipboardRecord / ApiKeyLabel 类型三处定义且已漂移 | `stores/clipboardStore.ts:84` |
| A6 | P2 | 架构 | clipboardStore.ts 一个文件导出两个 store，资源页状态藏在"剪切板"文件里 | `clipboardStore.ts:690-694` |
| A7 | P2 | 架构 | ResourcePage.tsx 1553 行单组件承载 8 类职责，与 ResourcePage/ 目录拆分半途而废 | `pages/ResourcePage.tsx` |
| A8 | P2 | 架构 | 三页面批量删除样板三份、GroupChips 两套分叉、粘贴双胞胎函数等重复 | `pages/*`、`stores/*` |
| B6 | P2 | 业务 | resource_watch 重建间隙丢事件，修订号心跳无法察觉静默丢失 | `resource_watch.rs:72-79` |
| B7 | P2 | 业务 | 键盘可达性缺口：卡片无法键盘粘贴，多数对话框不响应 Esc | `ClipboardCard.tsx:240` 等 |
| B8 | P2 | 业务 | settingsStore 装载失败被永久缓存，窗口存活期内不再重试 | `settingsStore.ts:77-108` |
| B9 | P2 | 业务 | 媒体服务对绝对路径不设限 + ACAO `*`，token 泄漏即可任意文件读取 | `media_server.rs:154,266` |
| E4 | P2 | 工程化 | build.yml 不跑任何测试，ci.yml 注释与事实相反 | `.github/workflows/*` |
| E5 | P2 | 工程化 | integrationRegression.test.ts 是 845 行字符串匹配"伪集成测试"（439 断言） | `src/integrationRegression.test.ts` |
| E6 | P2 | 工程化 | reqwest 为零使用的死依赖；tokio features 过宽 | `Cargo.toml:28-29` |
| E7 | P2 | 工程化 | asset 协议全盘放行 `**/*` + CSP connect-src 允许任意 HTTPS 外联 | `tauri.conf.json:31-38` |
| E8 | P2 | 工程化 | bundle.targets 与实际产物脱节；无更新器、产物未签名、无 changelog | `tauri.conf.json:43` |
| E9 | P2 | 工程化 | 版本四处手工同步，历史上有漏改先例 | `59f86a8` 等 |
| F4 | P2 | 结构 | 测试文件 4 种放置策略并存；多文件远超自定 400 行拆分阈值 | `src/styles/windowChrome.test.ts` 等 |
| F5 | P2 | 结构 | 本机专用 dev.cmd 与 .superpowers 过程产物入库 | `copy-creator/dev.cmd:8` |
| A9 | P3 | 架构 | 资源分组选中态三处重复维护，历史上已因此出过 bug | `ResourcePage.tsx:110` 等 |
| P9 | P3 | 性能 | 列表无虚拟化（已有 content-visibility / 懒加载缓解，暂可接受） | 全局 |
| P10 | P3 | 性能 | 短语缩略图无缓存；隐藏面板 10s 轮询空转；CSS 单文件 116KB 全窗口加载 | `phraseStore.ts:134` 等 |
| E10 | P3 | 工程化 | 生成器无 package.json 入口；vite build 零显式配置；无 packageManager 字段 | `package.json`、`vite.config.ts` |
| F6 | P3 | 结构 | components/ 平铺与 barrel 风格不统一；logo_top.png 462KB 偏大 | `src/components/` |

---

## 三、分维度详细分析

### 3.1 业务实现（B）

#### B1【P0】存储"迁移"不复制任何业务数据，与 UI 承诺相反；新库 schema 还缺 4 列

**现象与证据**：设置页向用户承诺"选择「迁移」：数据将复制到新位置"（`src/i18n/zh-CN.json:47`），但 `migrate_storage`（`src-tauri/src/db.rs:3717` 起）实际只做两件事：在新位置建**空库**（仅建表），再复制 settings 表（`db.rs:3813` 附近注释 "Copy settings to new DB"），随后直接 `*conn = new_conn`（`db.rs:3838` 附近）切换连接。整个函数体没有任何一条 `INSERT INTO clipboard_records/phrases/... SELECT ...` 业务数据复制语句（已核实 3717-3806 行区间）。

同时，新建库的 `clipboard_records` 建表语句（`db.rs:3743-3756`）只有 12 列，缺少 `init_db` 在旧库上以 ALTER 补充的 `resource_note`、`resource_external`、`use_count`、`touched_ms`（`db.rs:1492-1525`）。而主列表查询（`db.rs:2237`）与资源列表查询（`db.rs:2041-2043`）都引用这些列。

**影响**：用户确认"迁移"后，全部剪切板历史、快捷短语、API Key 标签被静默抛弃；图片附件目录不搬迁，旧图片记录路径全部失效。且迁移完成那一刻起到重启前，主列表命令因缺列报 "no such column"，应用半瘫痪。

**建议**：
1. 把 `init_db` 的建表 + ALTER 迁移提取为单一 `fn ensure_schema(&Connection)`，`migrate_storage` 建新库时复用（该文件里 schema 文本已存在 4 处拷贝：约 1298、3743、6079、8444 行，这是漂移的根源）；
2. 迁移改用 `ATTACH DATABASE` + 逐表 `INSERT INTO main.<t> SELECT * FROM aux.<t>`（7 张表都很小），资源库 / 图片附件目录用 `std::fs` 复制搬迁；
3. 新库完整建好、数据复制完成并校验行数后，再原子切换 `*conn`；
4. 若短期来不及修复，至少把 `migratePrompt` 文案改为"仅迁移设置"，停止误导用户。

#### B2【P0】`make_key_preview` 按字节切片，非 ASCII 内容必然 panic，且发生在持锁路径

**现象与证据**（已复核原文）：

```rust
// db.rs:57-65
pub fn make_key_preview(content: &str) -> String {
    let c = content.trim();
    if c.len() >= 12 {
        format!("{}...{}", &c[..8], &c[c.len() - 4..])
```

`c.len()` 是字节数，`&c[..8]` 在字节 8 落入多字节 UTF-8 字符内部时 panic。而 `is_api_key`（`db.rs:26-36`）的判定只要求 20-200 字节、以 `sk-` 等前缀开头，**不排除中文内容**。调用点有三处：`clipboard.rs:33`（采集线程）、`db.rs:2370`（`enrich_api_key_records`，在 `get_clipboard_records` 持 `DbState.conn` 锁时执行）、`db.rs:5232`（`check_api_key` 命令）。

**影响**：一条"复制了含中文的 sk- 前缀文本"即可引爆三条引线——① 采集线程 panic 后循环静默死亡，剪切板永久停采；② 列表查询 panic 毒化 `Mutex<Connection>`，之后所有 DB 命令返回 poisoned lock，数据层瘫痪至重启；③ 脏记录已入库，每次打开列表都复现 ②。

**建议**：改为字符安全实现（`c.chars().take(8).collect::<String>()` + 反向取 4），补 CJK 用例单测；同时按 B3 给采集线程加 panic 隔离。约 20 行改动，是全库性价比最高的修复。

#### B3【P1】采集 / 资源监听线程无 panic 隔离

**现象与证据**：`clipboard.rs:1110-1120` 的 monitor 主循环是裸 `loop {}`，无 `catch_unwind`；panic 输出随 `windows_subsystem = "windows"` 丢失，用户侧表现就是"剪切板不记录了"，无任何提示。对照之下 `shortcut.rs:1643-1656` 的 win_hook 分发线程已做 `catch_unwind` + 看门狗重装，同一仓库内标准不一致。

**影响**：B2 之外的任何 panic（arboard 平台层 bug、serde 毒化）都会让采集永久停摆。resource_watch 的 `watch_loop` 同理。

**建议**：两处循环体包 `catch_unwind`，panic 时 `log::error` 并 sleep 后重建循环，与 win_hook 的既有做法对齐。

#### B4【P1】粘贴失败被静默吞掉，无任何用户反馈

**现象与证据**：`src/pages/ClipboardPage/index.tsx:91-95` 调用 `void pasteRecord(record)` 不检查结果；`pasteRecord` 失败仅 `console.error` 后返回 false（`clipboardStore.ts:583-586`）；径向菜单 `RadialMenu/index.tsx:816` 同样不检查。对比资源页已有完整反馈机制（`ResourcePage.tsx:612-615` 的 `showFeedback("copyFailed")`）。

**影响**：这是本应用的核心动作。剪贴板写入失败时用户以为已粘贴，去目标应用 Ctrl+V 会贴出旧内容——静默失败直接造成用户数据事故。

**建议**：把资源页现成的 `showFeedback` 模式移植到剪切板页（样式已存在）；径向菜单失败时保留菜单可见或至少发 `debug_log`。半天工作量。

#### B5【P1】domain 层硬编码中文兜底文案，英文界面直接漏出

**现象与证据**（已复核）：`domain/records.ts:76` `record.type === "link" ? "链接内容" : "文本内容"`；`domain/records.ts:98` `.replaceAll("\uFFFC", "[图片]")`；`domain/recordView.ts:73` `recordDisplayName(record, "图片")`。同文件 `records.ts:146` 注释自己说"imagePlaceholder 由调用方传入（如 t(...)），domain 不依赖 i18n"，但所有 `buildRecordView` 调用方（`ClipboardPage/index.tsx:144`、`ResourcePage.tsx:1364`）均未传任何 option。

**影响**：这些字符串进入资源卡片标题、径向菜单条目与摘要，英文用户看到中文。

**建议**：给 `BuildRecordViewOptions` 补 `imagePlaceholder` / `fallbackTitle` 字段，由调用方经 `t()` 传入——机制已在，只差接线。

#### B6【P2】resource_watch 重建间隙丢事件，且修订号心跳无法发现"静默丢失"

**现象与证据**：`resource_watch.rs:72-79` 路径切换 / 失败重建时先 `drop(watcher.take())` 再 `new_watcher.watch(...)`，间隙内的文件系统事件无人接收；而 `RESOURCE_LIBRARY_REVISION` 修订号只在防抖冲刷时自增，事件完全丢失时修订号不变，前端 10 秒心跳对账无从察觉。

**影响**：切换资源库路径或监听短暂失败瞬间移入的文件不补录、不置顶，直到下次启动对账才被发现。

**建议**：watcher 重建成功后无条件触发一次 `Changed` 信号（冲刷逻辑幂等，代价一次重扫）；或增加 60s 级低频兜底重扫，把"自愈"从事件通道解耦。

#### B7【P2】键盘可达性缺口：核心动作无法用键盘完成

**现象与证据**：`ClipboardCard.tsx:240-244` 与 `PhraseList.tsx:150-158` 的卡片根节点只有 `onClick`，无 `tabIndex` / `role` / `onKeyDown`——剪切板页和快捷输入页的"粘贴"对键盘用户完全不可达（对比 `ResourceCard.tsx:140-149` 已有正确实现）。`ConfirmDialog.tsx:18-41` 无 Esc、无 autoFocus、无焦点管理；`ResourcePage.tsx:1037-1282` 三个手写对话框均不响应 Esc（而 `ClipboardCreateDialog/index.tsx:304-318` 有，标准不一）。

**建议**：卡片根节点补 `role="button" tabIndex={0}` + Enter/Space；抽一个 `useDialogBehavior(onClose)`（Esc + 初始焦点）供 ConfirmDialog 与三个对话框复用。

#### B8【P2】settingsStore 装载失败被永久缓存

**现象与证据**：`settingsStore.ts:77-82` 用 `settingsLoadPromise` 做窗口级幂等装载，但 `doLoadSettings` 的 `catch`（`:106-108`）吞错后 Promise 正常 resolve，缓存不失效。

**影响**：后端启动竞态下首次 `get_all_settings` 失败，主题 / 语言 / 排序整窗回落默认值直到重启；剪切板加载的 1.5s 竞速兜底（`clipboardStore.ts:334-339`）也依赖这次装载。

**建议**：失败路径置 `settingsLoadPromise = null` 允许重试（`domain/mediaUrl.ts:34-39` 已是正确示范）。

#### B9【P2】媒体服务与图片命令对"绝对路径"不设限，token 泄漏即可任意文件读取

**现象与证据**（已复核）：`media_server.rs:266-268` 对绝对路径原样返回：

```rust
let path = Path::new(value);
if path.is_absolute() {
    return Some(path.to_path_buf());
}
```

配合响应头 `Access-Control-Allow-Origin: *`（`media_server.rs:154`，注释说明是为了 video 帧导出 canvas 所必需）。同类：`get_image_base64` 走的 `resolve_storage_path`（`db.rs:321-324`）同样放行绝对路径。

**影响**：本地回环 + 每次启动随机 token 的设计基本够用，风险需要"渲染进程已被注入"这一前提；但一旦发生（应用会渲染外部文件内容），攻击者可借该服务读取磁盘任意文件，不限资源库。

**建议**：`resolve_media_file` 只放行资源库目录与存储目录内的路径（复用 `normalize_relative_path`，拒绝 `..`）；`get_image_base64` 拒绝存储目录之外的绝对路径；ACAO 收紧为回显 Origin 校验（保留 canvas 导出能力）。

#### B10【P3】数据模型与硬化收尾项（三小项合并）

- **外键从未启用**：`db.rs:1309-1324` 声明了 `FOREIGN KEY ... ON DELETE CASCADE`，但 `init_db` 的 PRAGMA（`db.rs:1277-1279`，已复核）只有 journal/synchronous/cache_size，无 `foreign_keys=ON`；级联删除实际靠 `delete_phrase_group` 手写两条 DELETE。孤儿行无人拦截。
- **去重窗口依赖 RFC3339 字符串比较**：`clipboard.rs:942-955` 的 2 秒去重窗口用 `created_at >= ?3` 文本比较，而 `to_rfc3339()` 的秒内小数位数不固定，边界偶发误判；短语按 `last_used_at` 文本排序同理（`db.rs:2436-2440`）。建议新增 `created_ms INTEGER` 列统一毫秒比较（可与 P3 的索引改造一并做）。
- **IPC / 媒体服务收尾**：Unix socket 退出无清理钩子（`ipc.rs:97`、`tray.rs:50`）；media_server 每连接 `thread::spawn` 无并发上限、无写超时（`media_server.rs:70-72`）。均为低风险项，顺手加固。

### 3.2 性能（P）

#### P1【P1】剪贴板 800ms 轮询无条件全量读取图片

**现象与证据**（已复核）：`clipboard.rs:1113` 每 800ms 一轮；`clipboard.rs:1132-1135` 每轮都新建 arboard 实例并 `get_image()`，仅靠 `step_by(64)` 采样哈希判重——即图片**未变化**也要每轮完整拷贝一次 RGBA。

**影响**：剪贴板里驻留一张 4K 截图时，持续 ≈40MB/s 的 memcpy + 哈希；Windows 上还伴随每轮 OLE OpenClipboard/CloseClipboard，与其他应用的剪贴板操作互相争锁。这是常驻后台应用，代价持续发生。

**建议**：最小改动版——图片哈希命中后，后续轮次跳过 `get_image()`，仅在文本 / 文件检测失败时每 N 轮复查一次图片；中期事件化（Windows `AddClipboardFormatListener`，Linux X11 剪贴板事件），轮询只做兜底。

#### P2【P1】资源列表查询：无分页全量加载 + 逐行 stat + 内存过滤排序，全部在数据库锁内

**现象与证据**（已复核 stat 部分）：`db.rs:2036-2044` 查询无 `LIMIT`，全部行读出后**逐行** `std::fs::metadata(path)`（`db.rs:1997`，已复核原文），`db.rs:2131` 起在 Rust 里做搜索 / 排序，到 `db.rs:2184-2187` 才做 `limit.unwrap_or(200)` + skip/take。

**影响**：数万文件的资源库每次切分组 / 搜索 = 全表读取 + N 次 stat + 全量字符串 `to_lowercase().contains`，且整段持有 `DbState.conn` 锁，拖慢所有并发 DB 操作。

**建议**：size / modified 在 `sync_resource_library` 与监听补录时写库（加 `resource_size`、`resource_modified` 两列），查询不再碰文件系统；搜索下推为 SQL `LIKE`（与主列表同款 ESCAPE 写法）；分页 `LIMIT/OFFSET` 下推。

#### P3【P1】每小时清理任务：非 sargable 条件 + 无索引 content 逐条 COUNT

**现象与证据**（已复核）：`db.rs:1637-1640` `WHERE datetime(created_at) < datetime('now', ?1)` 对列包函数，使 `idx_clipboard_created_at` 失效；`db.rs:1677` 对每张待删图片执行 `SELECT COUNT(*) > 0 ... WHERE content = ?1`，content 无索引，每次全表扫描。该任务由 `lib.rs:187-190` 每小时触发一次。

**影响**：记录上万后，每次清理造成周期性卡顿尖峰，全部发生在连接锁内。

**建议**：增加整数毫秒列 `created_ms` 并建索引（同时解决 B10 的字符串比较问题）；`content` 建索引（它同时服务于采集去重 `db.rs:947-957` 与删除去重 `db.rs:2949` 的等值查询）；SELECT+DELETE 合并为 `DELETE ... RETURNING`。

#### P4【P1】整组粘贴 / 拖出触发整个资源库递归扫描

**现象与证据**：`db.rs:2624`（`touch_resource_group_usage_internal` 内）`for entry in scan_resource_files(&root)` 全库扫描，只为找出分组内未入库文件做补建；`sync_resource_library`（`db.rs:1726`）与分组树（`db.rs:4017`）同模式。

**影响**：大库下每次整组粘贴 / 拖出（高频操作）同步阻塞数百毫秒到秒级。

**建议**：只扫描目标分组子目录（`root.join(folder)`）而非全库；touch 路径上的"补建入库"本可延后交给 resource_watch 的常规补录。

#### P5【P1】卡片入场动画按全列表 index 线性延迟，深翻页卡片"隐身"数十秒

**现象与证据**（已复核）：`ClipboardCard.tsx:243` `style={{ ..., "--enter-delay": index }}`，`styles/components.css:164` `animation-delay: calc(var(--enter-delay, 0) * 55ms)`，配合 `cardEnter ... both`（fill-mode 停在不可见起始帧）。`index` 是全列表下标，store 上限 2000 条（`clipboardStore.ts:282`）。

**影响**：点 4 次"加载更多"到第 500+ 条时，新卡片要等 ≈27s 才出现；同时浏览器排队上千个延迟动画。

**建议**：一行修复——`Math.min(index, 20)` 只让首屏有入场延迟；或改用 IntersectionObserver 触发。

#### P6【P1】三个列表容器全量 store 订阅 + 卡片零 memo + 径向菜单热点路径无 memo

**现象与证据**：
- `ResourcePage.tsx:70-84` `useResourceStore()` 无选择器全量解构（含用不到的缩略图缓存）；`ResourcePage.tsx:339` `splitResourceColumns(...)` 未 memo；`ResourcePage.tsx:1364` `buildRecordView(record)` 在渲染循环内联执行，每次渲染为每条记录新建对象；
- `PhrasePage/index.tsx:69-95`、`ClipboardPage/index.tsx:28-44` 同病；
- `ClipboardCard.tsx:510-512` 导出层是无 memo 的纯转发包装；
- 径向菜单 `RadialMenu/index.tsx:1371-1506` 的 `filteredRecords` 与 `items`（最多 2000 条映射 + 日期解析）是组件体裸代码，无 useMemo，而 hover 每次 mousemove 都 setState 触发整树重渲；`ResourceItemVisual.tsx:82-87`（已复核）`RadialImageBanner` 逐条全量订阅 store + `records.find`，列表查找 O(n²)。

**影响**：loading 翻转、每次搜索去抖后的键、每次 append 都触发整页（含上百张卡片与媒体组件）重渲，WebView 环境下卡顿可感；径向菜单是高频交互入口，开销直接落在打开延迟上。

**建议**：页面容器改选择器订阅（径向菜单 `RadialMenu/index.tsx:1365-1369` 已是正确示范）；`columns` / `views` / `items` 补 useMemo；`ClipboardCard` / `ResourceCard` 包 `React.memo`；`RadialImageBanner` 改接收 `path` prop，去掉 store 订阅。

#### P7【P2】记录更新事件触发整列表重载回第一页

**现象与证据**（已复核）：`clipboardStore.ts:288-292` `listen("clipboard-record-updated", () => get().loadRecords())`，`loadRecords()` 无参即从 offset 0 重载。触发链包括：径向菜单粘贴 → 后端 `touch_clipboard_usage` → 广播 → 主窗重载。

**影响**：用户翻页浏览到深处时，列表被压回 120 条、滚动位置丢失，体感像"跳回顶部 / 丢数据"。

**建议**：单条更新改定向 patch（参照 `updateRecordLabel` 的 `:508-515` 模式）；确需重载时保留 offset 并按新总数重算 `hasMore`。

#### P8【P2】图片链路重复编解码与大对象克隆

**现象与证据**：`clipboard.rs:1199` 捕获时先 RGBA→PNG 编码落盘，再把刚编码的 PNG **重新解码**一次只为生成缩略图（手里本就有解码好的 rgba 缓冲；`clipboard.rs:907-922` 的 `import_image_file` 是正确写法）；`paste.rs:996` `cache_image(path.clone(), (*rgba).clone(), ...)` 把 4K 图 ≈33MB 像素整体克隆入缓存。

**建议**：缩略图直接从已有 rgba 缓冲用 `image::imageops::thumbnail` 缩放；`cache_image` 改收 `Arc<Vec<u8>>` 消除克隆。

#### P9【P3】列表无虚拟化（暂可接受）

三个窗口列表上限 2000 条全量挂载，无 react-window / virtuoso。已有缓解：`resource.css:826`、`radial-menu.css:410` 的 `content-visibility: auto`，图片真懒加载（`ResourceMedia.tsx:97-126`）。建议短期接受现状；若做，优先径向菜单与剪切板页，瀑布流资源页可只做分块渲染。

#### P10【P3】三处小的性能打磨

- 短语图片缩略图无进程内缓存（`phraseStore.ts:134-136` 直接 invoke；`PhraseList.tsx:50-67` 挂载即请求），对比资源区已有 240 条 LRU（`ResourceMedia.tsx:59-69`），切分组即重复解码——复用 LRU 模式即可；
- 资源页 10s 修订号轮询在面板隐藏（display:none）时仍运行（`App.tsx:36-47` 保挂载 + `ResourcePage.tsx:216-233` setInterval），回调里先判断面板可见性；
- CSS 共 7965 行打进单一 115.9KB 文件随三个窗口全量加载（`main.tsx:3` 在 root 分支之前 import），JS 已按窗口分包而 CSS 未对齐——radial-menu.css / settings.css 改由对应 root 组件 import，Vite 自动分包。

### 3.3 架构（A）

#### A1【P2】db.rs 巨石：70 个命令、7 张表、约 6 个业务域、4 份 schema 拷贝

**现象与证据**：`grep -c '#\[tauri::command\]' db.rs` = 70（已核实）；`lib.rs:337-436` 注册的 98 个命令中 70 个来自 db；`CREATE TABLE` 文本出现在约 1298 / 3743 / 6079 / 8444 四处（B1 的缺列事故正是这 4 份拷贝漂移的直接恶果）。业务域至少含：剪贴板记录、短语、资源库（文件系统 + 索引）、快速输入文件、API Key 标签、设置与存储迁移、缩略图缓存。约 3040 行测试与生产代码混排。`DbState { conn: Mutex<Connection> }` 单连接全局串行，所有命令排队。

**建议**：按域机械拆分为 `db/mod.rs`（DbState、ensure_schema、prune）+ `db/clipboard.rs` / `phrase.rs` / `resource.rs` / `labels.rs` / `settings_storage.rs` / `thumbs.rs`——命令函数原样搬迁，`generate_handler!` 路径不变，零行为变更；`ensure_schema` 成为 schema 唯一事实源。中期可引入 `r2d2_sqlite` 连接池，读命令并行化（WAL 已开，读并发安全）。

#### A2【P2】shortcut.rs 混装 4 类职责

**现象与证据**：1872 行内并存——全局快捷键注册 / 匹配（`register_keyboard_shortcut`，`:1136`）、径向菜单窗口几何与层级管理（`show_radial_menu`，`:677-834`，含手拼 X11 `_NET_ACTIVE_WINDOW` 事件的 mutter 防抢占，`:464-535`）、新建窗口几何（`:836-1015`）、Windows 低级键盘钩子（`mod win_hook`，`:1352-1757`，含 AttachThreadInput 与看门狗）。

**影响**：改窗口层级约定要动"快捷键"文件；X11 FFI 与 Win32 钩子互相隔不应有的上下文；新平台分支只能继续往里塞。

**建议**：拆为 `shortcut.rs`（注册 / 匹配）、`radial_window.rs`（径向窗口几何 / 层级 / 停泊 / X11 激活）、`win_hook.rs`（cfg(windows)）、`clipboard_create_window.rs`；纯机械搬迁，调用点改名即可。

#### A3【P2】双平台窗口模型靠标志位与 cfg 交错维系

**现象与证据**：`#[cfg(...)]` 分布——radial_drag.rs 51 处、shortcut.rs 40 处、paste.rs 21 处；径向菜单 Linux"常驻映射 + 屏幕外停泊" vs Windows"hide/show"两套模型（`shortcut.rs:737-750`）；`RADIAL_MENU_SHOWN` / `RADIAL_ORIGIN_OWN_WINDOW` / `LinuxDragState` 等标志位跨文件同步（翻转点散落 `shortcut.rs:706,797` 与 park 流程）。

**影响**：分支本身有注释和单测支撑、质量尚可，但每个新窗口行为都要在两套模型下各验一遍，回归面大。

**建议**：抽象 `RadialWindowController` trait（show / hide / park / is_shown，两平台各一实现），调用方不再写 cfg；至少把 `RADIAL_MENU_SHOWN` 的翻转收敛到单一函数。

#### A4【P2】RadialMenu 绕过 domain 出口直接 invoke，预览判定与 domain/preview.ts 平行实现

**现象与证据**（已复核）：`RadialMenu/index.tsx:504` 直接 `invoke<string>("read_quick_input_text_preview", ...)`，而 `domain/mediaAssets.ts:13` 已有唯一出口 `readQuickInputTextPreview`（文件头注释明确"UI 层禁止直接 invoke"）；`index.tsx:456-517` 的资源分支自建 kind→segments 判定，与 `domain/preview.ts:107-140` 已收口的同一规则平行。且 `eslint.config.js:34-38` 的禁止清单包含 `read_clipboard_text_preview`、`read_resource_text_preview`（已核实），**唯独漏了 `read_quick_input_text_preview`**——lint 拦不住这处绕行。

**影响**：恰好复现了架构方案 §1 要根治的"同一条规则各写各的"；预览行为改动需同步两处。

**建议**：改 import domain 封装（一行）；资源分支判定并入 `domain/preview.ts`；把 `read_quick_input_text_preview` 补进 eslint 禁止清单。

#### A5【P2】核心类型三处定义且已漂移

**现象与证据**：`src/types/index.ts:10-42` 是权威定义（含 `resource_modified?: number`）；`stores/clipboardStore.ts:84-113` 手写副本**缺少 `resource_modified`**，另复制了一份 `ApiKeyLabel`；`ClipboardCreateDialog/index.tsx:17-29` 第三份 `StashRecord` 子集。`domain/records.ts:43-49` 的 `resourceMediaVersion`（媒体缓存失效键）依赖 `resource_modified`，store 类型却看不见该字段。

**影响**：后续新增后端字段极易漏改 store 副本；类型系统对 store "隐瞒"版本字段，容易写回 URL 缓存不失效类回归。

**建议**：clipboardStore 删除本地 interface 改 `import type`；`StashRecord` 用 `Pick<ClipboardRecord, ...>` 表达。

#### A6【P2】clipboardStore.ts 一个文件导出两个 store

**现象与证据**（已复核）：`clipboardStore.ts:690` `export const useClipboardStore = createRecordsStore()`，`:694` `export const useResourceStore = createRecordsStore()`——资源页（含 ResourceDetailPage、径向菜单资源 tab）的状态全在"剪切板"文件里。

**影响**：违背文件名直觉，排查资源 bug 容易找错文件；文件承担双份职责也阻碍后续拆分。

**建议**：改名为 `recordsStore.ts`（反映"记录工厂"本质），或把 `useResourceStore` 拆出独立的 `resourceStore.ts`、共享工厂上移。顺带把 `domain/mediaAssets.ts` 更名对齐术语（全库其余处均用 resource/mediaKind，"asset" 仅此一处）。

#### A7【P2】ResourcePage.tsx 单组件承载 8 类职责

**现象与证据**：1553 行的函数组件内含工具栏 + 库路径设置弹层（`:939-983`）、分组管理 DnD 对话框（`:1037-1117`）、新建 / 重命名对话框（`:1119-1175`）、移动分组对话框（`:1177-1282`）、批量选择（`:764-823`）、详情路由 + popstate/Esc（`:617-762`）、10s 轮询（`:216-233`）、瀑布流列表（`:1286-1400`），共 30+ useState。同目录 `ResourcePage/` 已拆出 6 个子组件但容器逻辑未动。

**影响**：任何局部状态（如输入分组名）变化都重跑整页渲染流水线（叠加 P6 的重渲问题）；与已拆分的 PhrasePage 结构不对称。

**建议**：照 PhrasePage 模式把三个对话框、设置弹层拆为独立文件（各含自己的状态），容器只留数据流与列表；入口收敛为 `ResourcePage/index.tsx`（见 F4）。

#### A8【P2】跨页面 / 跨 store 的结构性重复

**现象与证据**：
- `handleDeleteSelected` 三份近似拷贝：`ClipboardPage/index.tsx:232-259`、`PhrasePage/index.tsx:413-423`、`ResourcePage.tsx:797-826`；
- `PhrasePage/GroupChips.tsx`（192 行）与 `ResourcePage/ResourceGroupChips.tsx`（389 行）平行存在且已分叉；
- `pasteRecord` / `pasteRecordTerminal` 逐行相同仅差 terminal 标志（`clipboardStore.ts:552-622`）；短语"最多使用重排"乐观更新两份完全一样（`phraseStore.ts:292-318`、`337-363`）；`loadAllRecords` 分页循环两份（`clipboardStore.ts:428-446`、`461-478`）；
- 视频封面帧组件约 120 行近乎逐行复制两份（`FileMediaPreview.tsx:69-208` vs `ResourceMedia.tsx:308-440`，IntersectionObserver / seeked 取帧 / 导出持久化三段一致）。

**建议**：抽 `useDeleteSelected(deleteFn, confirmKey)` hook；`pasteRecord(record, terminal)` 合并；重排抽纯函数 `bumpPhraseToTop`；`VideoPosterFrame` 提为共享组件。均为不改行为的等价变换，配现有测试兜底。

#### A9【P3】资源分组选中态三处重复维护

**现象与证据**：`ResourcePage.tsx:110` 本地 state 与 store 的 `setStoreResourceGroup` 成对双写（六处）；`RadialMenu/index.tsx:195` 又一份本地 state + ref；`collapsedGroupPaths` 三处各一份。`ResourcePage.tsx:412-430` 的自愈 effect 注释显示曾因此出过 bug。

**建议**：分组选中态收敛进 records store（字段已存在），页面只读；折叠态可保留本地。

### 3.4 工程化（E）

#### E1【P1】TypeScript 未开 strict，且 CI 不做类型检查

**现象与证据**（已复核）：`tsconfig.app.json` 全文无 `strict` / `noImplicitAny` / `strictNullChecks`，仅有 noUnused 系与 noFallthrough；`ci.yml` 只有 `pnpm lint`（eslint 用 recommended 非类型检查变体）与 `pnpm test`（vitest 默认不做类型检查）；`tsc -b` 只挂在 `build` 脚本上，仅在发版构建时执行。

**影响**：隐式 any 与 null/undefined 解引用全部放行；类型错误可以合进 main，直到下一次发版才暴露并阻塞发布。对一个频繁操作记录视图模型的前端是重大类型安全缺口，且修复成本随代码量线性上涨。

**建议**：ci.yml 增加独立 `pnpm typecheck`（`tsc -b --noEmit`，秒级成本）；随后分模块渐进开启 `strict: true`，评估 `noUncheckedIndexedAccess`；中期把 eslint 迁到 `recommendedTypeChecked`。

#### E2【P1】CI 无 Windows job，Windows 专属代码只在发版日编译

**现象与证据**（已复核）：`ci.yml:13` 单 `runs-on: ubuntu-latest`；`build.yml` 仅 `workflow_dispatch` 与 `v*` tag 触发。而 Cargo.toml 有大量 Windows 专属依赖（winreg、windows crate 的 OLE / Shell 特性，径向菜单 OLE 拖出、低级键盘钩子）。

**影响**：Windows 编译错误可以畅通合进 main，直到打 tag 时在发布构建中炸掉，阻塞发版（这正是"径向条带 Windows 改 resize 展开"这类提交只能靠发布后人工回归的原因）。

**建议**：ci.yml 的 test job 加 `windows-latest` matrix，至少 `cargo check` + `cargo test --lib`（Linux 系统依赖步骤用 `if:` 隔离）；前端测试留 ubuntu 即可。

#### E3【P1】双 lockfile 并存，package-lock.json 停在 v0.1.13 仍被跟踪

**现象与证据**（已核实）：`copy-creator/` 同时跟踪 `pnpm-lock.yaml`（75KB，权威——README 与 CI 均用 pnpm `--frozen-lockfile`）与 `package-lock.json`（148KB，内部 version 停在 `0.1.13`，抽查 react/vite/eslint 等版本全部漂移）。

**影响**：任何按 npm 习惯 `npm install` 的贡献者会装出一棵与 CI 差一个月的依赖树，问题极难排查。

**建议**：`git rm` package-lock.json；package.json 加 `"packageManager": "pnpm@<版本>"` 与 `preinstall: "npx only-allow pnpm"` 防呆。十分钟改动，消除最大的一颗"新人踩坑雷"。

#### E4【P2】build.yml 不跑任何测试，ci.yml 注释与事实相反

**现象与证据**（已复核 build.yml 步骤清单）：从 checkout 到 tauri-action 发布之间没有任何 lint / test 步骤；而 `ci.yml:1-3` 注释声称"发布构建（build.yml）保留测试步骤作为最后防线"。两 workflow 均无 `concurrency` 取消配置，actions 按 tag 引用未 pin SHA，无 clippy / fmt。

**影响**：打 tag 后无人验证测试是否通过就把二进制发布给用户；注释误导维护者以为有防线。

**建议**：build.yml 在 tauri-action 之前补 lint + test 两步（或 tag 先触发 CI 通过再 build）；加 `concurrency`；择机加 clippy；修正 ci.yml 注释。

#### E5【P2】"伪集成测试"承担了无覆盖模块的全部回归防线

**现象与证据**：`src/integrationRegression.test.ts` 845 行、439 个断言，全部是对 TS/CSS/**Rust 源码**的字符串匹配（如断言 Rust 源码含某条 SQL 字符串、CSS 含某个 rgba 值）。前端另零 DOM / 组件级测试（无 jsdom / testing-library），vitest 无独立 test 配置；Rust 侧 118 个测试分布不均（db.rs 73 个、autostart / resource_watch / tray / ipc / lib 零覆盖），且只在 Linux 执行。

**影响**：这类测试极脆弱（重命名、重排属性、格式化即红）且通过也不证明行为正确；但它目前是 resource_watch、粘贴路径等无真实测试模块的唯一防线，不能一删了之。公允地说其注释质量很高，是一份"回归原因档案"。

**建议**：立即冻结其增量——新回归一律写成真实测试（组件行为 → jsdom + testing-library；DB 路径 → Rust 内存 sqlite，db.rs 已有成熟写法；resource_watch → notify + tempdir 真实触发）；存量按风险从高到低逐条迁出，字符串守卫只保留给"架构边界"类（与 architectureGuard 分工）；把 `test` 配置显式写进 vite.config。

#### E6【P2】reqwest 是零使用的死依赖

**现象与证据**（已核实）：`Cargo.toml:28` 声明 reqwest，但 `src-tauri/src/` 全量 grep 为空。附带 `tokio features = ["full"]` 远超实际所需。

**影响**：reqwest 连带 hyper + TLS 栈整套编进 release 二进制，拉长每次构建、增大产物、扩大供应链面。

**建议**：删除 reqwest（若为规划中的翻译功能预留，实现时再加）；tokio 收窄到实际用到的 features。

#### E7【P2】asset 协议全盘放行 + CSP connect-src 允许任意 HTTPS 外联

**现象与证据**（已复核）：`tauri.conf.json:36` `"allow": ["**/*"]`（asset 协议可加载磁盘任意路径）；`:31` CSP `connect-src 'self' https:`。capabilities 本身是收敛的，符合最小权限。

**影响**：与 B9 叠加——一旦渲染进程被注入，任意文件读取与数据外传门槛都很低。

**建议**：asset scope 收窄到资源库目录与 `$APPDATA/**`；connect-src 按实际需要白名单化（当前实际只需要 media server 的 localhost 与 dev server）。

#### E8【P2】发布链路：targets 与产物脱节、无更新器、未签名、无 changelog

**现象与证据**（已复核）：`tauri.conf.json:43` `"targets": ["deb", "appimage"]`，但 GitHub Releases 实际产出四件套（AppImage/deb/NSIS/MSI），配置名不副实；无 `.sig`、无 `TAURI_SIGNING_PRIVATE_KEY`、无 updater 配置；所有 release 的 body 是同一句静态文案，全仓无 CHANGELOG。

**影响**：用户无自动更新通道、无产物校验手段；每个版本改了什么无从得知。

**建议**：targets 显式写全消除歧义；中期引入 tauri-plugin-updater + 签名密钥，产物附校验和；CHANGELOG.md 从"git log 整理"起步即可。

#### E9【P2】版本四处手工同步

**现象与证据**：提交 `59f86a8`"发布 0.3.1，统一四处版本号"（上一条同类提交还是"统一三处"）——四处为 `package.json` / `Cargo.toml` / `Cargo.lock` / `tauri.conf.json`，纯手工 sed。漏改风险已真实发生过。

**建议**：写一个十几行的 release 脚本：读 package.json 版本 → 同步写四个文件 → 生成 changelog 草稿 → 打 tag，一步完成。

#### E10【P3】工程杂项三项

- 生成链路无 package.json 入口：改 `config/media-types.json` 后需知道裸跑 `node scripts/generate-media-types.mjs`（守卫测试会兜底报红，但不可发现）——加 `"gen:media-types"` / `"gen:check"` 脚本，build 前置 check；
- `vite.config.ts` 无 `build.target` / manualChunks / sourcemap 策略，打包目标走 vite 默认（对 WebKitGTK 旧版本有兼容风险）——按双平台实测定基线；
- 无 `packageManager` / `engines` 字段，pnpm 版本只在 CI 固定——本地大版本不一致时可能改写 lockfile 格式。

### 3.5 文件结构（F）

#### F1【P1】README 多处失实，LICENSE 缺失是合规硬伤

**现象与证据**（已核实）：
- `README.md:9` 与 `README_EN.md:9` 引用 `copy-creator/public/logo.png`，实际 `public/` 只有 `favicon.svg` 和 `logo_top.png`——GitHub 上 logo 裂图；
- 两份 README 链接 `[MIT 许可证](LICENSE)` 且挂 MIT badge，但仓库**没有 LICENSE 文件**——本项目 Fork 自他人项目，分发无许可证文本是合规硬伤；
- `README.md:117` 称 `copy-creator-ctl` 在"安装目录的 scripts/ 文件夹"，但 `tauri.conf.json` 的 bundle 段没有 `resources` 配置（已核实 grep 无此键），安装包不会包含该脚本——按 README 操作的 Ubuntu 用户找不到它；
- `copy-creator/README.md`（应用内）宣传"翻译"功能，但三个 tab 已无翻译入口，仅 db.rs 残留 `translation_history` 表；其快捷键指引与根 README 描述的是两代行为；
- 「项目结构」章节缺 `domain/`（架构核心层）、`hooks/`、仓库根的 `config/` + `scripts/`（生成链路源头）。

**建议**：logo 改引 `logo_top.png`；从上游补入 LICENSE；bundle.resources 加入 ctl 脚本或改 README 文案；应用内 README 删除或改为一行指回根 README；补全项目结构树并注明"改 media-types.json 必须重跑生成器"；确认翻译功能弃用后给出 `translation_history` 表的清理 / 迁移说明。

#### F2【P1】DOMAIN_ARCHITECTURE_PLAN.md 处于"半删除"状态，且被 4 处代码引用

**现象与证据**（已核实当前状态）：该文件仍被 git 跟踪（HEAD 中存在），工作区已删除且删除未暂存（`git status` 显示 ` D`）；同时它仍是"活文档"——`src/domain/README.md:3` 按名引用，`src/architectureGuard.test.ts:2`（§5.2）、`eslint.config.js:23`（§5.1）、`ci.yml:3`（§5.4）、`scripts/generate-media-types.mjs:6`（§5.2 规则 7）的注释都指向其章节号。

**影响**：现状让每个读仓库的人困惑；若完成删除则守卫规则的出处消失，产生一批悬空引用。

**建议**：二选一并执行到底——要么恢复提交（架构方案仍值得作为基线文档保留）；要么完成删除，把有效规则沉淀进 `src/domain/README.md`，同步改掉 4 处章节引用。**需要项目所有者决断**（排查期间工作区刚发生此删除，可能正是所有者在处理）。

#### F3【P1】"快捷短语"概念存在 4 套并行命名

**现象与证据**：同一功能——代码 / DB 用 `phrase`（`phrase_groups` / `phrases` 表、`phraseStore`）；UI 中文叫"快捷输入"（`zh-CN.json` tabs.phrases）；README 主章节叫"快捷短语"、径向菜单章节又叫"快捷输入"；IPC 与存储目录用 `quick_input`（`read_quick_input_text_preview`、`quick-input-files/`，db.rs 中出现 58 次）；英文 UI 叫 "Quick Input" 而 README 英文叫 "Quick Phrases"。跨代码、UI、文档、IPC 四层检索同一概念要换 4 个词。

**影响**：新贡献者和 AI 执行者极易漏改——架构方案 §1 列举的缺陷正是这类"找不全就漏改"。

**建议**：定一个准（建议代码 / 表保持 `phrase`，UI 文案与两份 README 统一为"快捷短语 / Quick Phrases"），`quick_input` 作为历史 IPC / 目录名在注释与文档中显式声明等价关系，新建接口不再使用。

#### F4【P2】测试放置 4 种策略并存；多个文件远超自定拆分阈值

**现象与证据**：15 个测试文件 4 种放法——`src/integrationRegression.test.ts`（src 根）、`src/architectureGuard.test.ts`（src 根）、`src/styles/windowChrome.test.ts`（**CSS 目录里的 TS 测试**，同时断言 radial-menu.css 与 lib.rs）、其余 12 个与被测文件同目录。巨型文件：`RadialMenu/index.tsx` 1922 行、`ResourcePage.tsx` 1553 行、`resource.css` 2365 行、`db.rs` 8510 行——而 `domain/README.md` 自定的拆分触发条件是"超过约 400 行"。另 `pages/ResourcePage.tsx` 与 `pages/ResourcePage/` 目录同名并存，`App.tsx` 的 import 实际解析到 1553 行的文件而非目录，读者会扑空。

**建议**：约定"单测与被测文件同目录，跨栈契约测试集中到 `src/testing/`"，迁入 integrationRegression / architectureGuard / windowChrome 三个并在 domain/README 写明规则；`ResourcePage.tsx` 移入 `ResourcePage/` 改名 `index.tsx` 作为唯一入口；巨石文件按 A1/A2/A7 的拆分方案分批处理（Rust 模块拆分零运行时成本，前端拆分配合 A7 一并做）。

#### F5【P2】本机专用脚本与过程产物入库

**现象与证据**（已核实 git ls-files）：`copy-creator/dev.cmd:8-9` 硬编码本机 VS 路径 `F:\install\L\VC\Tools\MSVC\14.43.34808` 与本机 SDK 版本，对其他机器直接无效；`copy-creator/.superpowers/sdd/fix-report.md` 是一次修复会话的报告。对比之下 `src-tauri/test.cmd` + `tests.manifest` 是通用且必要的（lib 测试的 manifest 嵌入），应保留。

**建议**：dev.cmd 改为从环境变量读取路径的模板，或移出版本控制（本地保留 + gitignore）；删除 `.superpowers/` 并补 ignore 规则。

#### F6【P3】仓库卫生收尾两项

- `components/` 16 个文件平铺 + 3 个子目录并存，`settings/` 有 barrel 其余直接深路径 import——约定"超过 3 个文件或含私有子组件即目录化"，风格二选一；
- `public/logo_top.png` 462KB 对一个侧栏 logo 过大——压缩或转 webp。

---

## 四、实施路线图

按"先止血、再补门禁、然后还性能债、最后还结构债"排序。每阶段独立可验证、可单独提交（符合仓库小阶段提交约定）。

### 阶段一：P0 止血（预计 1-2 天）

| 事项 | 内容 | 验证方式 |
|:--|:--|:--|
| B2 + B3 | `make_key_preview` 字符安全化 + CJK 单测；采集 / resource_watch 循环加 `catch_unwind` + 日志 | 新增 CJK 用例单测全绿；人工验证复制中文 sk- 文本后采集不停 |
| B1 | `ensure_schema` 单一事实源 + `ATTACH` 逐表复制 + 附件目录搬迁 + 行数校验后原子切换 | Rust 集成测试：迁移后 7 张表行数与旧库一致、列表命令可用；人工走一遍设置页迁移流程 |

### 阶段二：工程门禁补洞（预计 2-3 天）

| 事项 | 内容 | 验证方式 |
|:--|:--|:--|
| E3 + E6 | 删 package-lock.json、加 only-allow 与 packageManager；删 reqwest、收窄 tokio features | `cargo check` 通过、二进制体积对比；CI 绿 |
| E1 | CI 加 typecheck 步骤；制定 strict 分模块开启计划并启动 | ci.yml 出现 typecheck 步骤且绿 |
| E2 + E4 | ci.yml 加 windows matrix（check + test --lib）；build.yml 补 lint/test 前置；修 ci.yml 失实注释；加 concurrency | PR 上出现 Windows job；本地模拟 tag 触发可见测试步骤 |
| F1 + F2 + F5 | README 四处修正 + 补 LICENSE；DOMAIN_PLAN 恢复或完成删除（改 4 处引用）；dev.cmd 泛化、删 .superpowers | 页面渲染检查 README；`git grep` 无悬空 §引用 |

### 阶段三：性能与体验（预计 1 周）

| 事项 | 内容 | 验证方式 |
|:--|:--|:--|
| P2 + P3 + P4 | created_ms 列 + 索引（兼解决 B10 去重比较）、content / resource_path 索引；资源列表 size/modified 写库、搜索下推 SQL、分页下推；整组操作只扫目标子目录 | Rust 侧 1 万行种子数据基准对比（列表 / 清理 / 整组粘贴耗时）；既有 73 个 db 测试全绿 |
| P1 | 图片哈希命中后跳过后续轮次读取 | 日志计数对比图片驻留期间的 get_image 次数 |
| P5 + P6 + P7 | 动画延迟上限；三页选择器订阅 + memo；径向 items memo + Banner 去 store 订阅；记录更新定向 patch | React DevTools Profiler 对比渲染次数；人工验证深翻页不再跳顶 |
| B4 + B5 + B8 | 粘贴失败反馈；domain 文案参数化；settings 装载失败可重试 | 切英文界面无中文漏出；断开后端模拟首次装载失败可恢复 |

### 阶段四：结构整固（1-2 周，分批独立提交）

| 事项 | 内容 | 验证方式 |
|:--|:--|:--|
| A1 | db.rs 按域拆分 + ensure_schema 收敛（纯机械搬迁） | 全部命令注册不变、118 个 Rust 测试全绿、手工冒烟 |
| A2 + A3 | shortcut.rs 拆分；RadialWindowController trait 化 | 双平台径向菜单呼出 / 停泊 / 拖出回归 |
| A4 + F3 | RadialMenu 预览并入 domain、eslint 清单补漏；命名统一 | 架构守卫新增规则生效；`git grep quick_input` 仅剩声明等价关系的注释 |
| A5-A9、A8 | 类型三处合一；store 改名 / 拆分；重复逻辑合并（粘贴双胞胎、批量删除、GroupChips、VideoPoster、分页循环） | vitest 全绿 + 新抽纯函数的单测 |
| E5 + F4 | 冻结伪集成测试增量；建 `src/testing/`；ResourcePage 入口收敛 | 新测试放置规则入文档；测试清单归位 |
| B6 + B7 + B9 + B10 | watcher 重建补信号；键盘可达性 + dialog hook；媒体路径白名单 + ACAO 收紧；FK / 硬化收尾 | 资源库路径切换压力测试；键盘走查；越界路径请求 404 |
| E7-E10、F6、P8-P10 | 安全配置收窄、发布链路（updater/签名/changelog/release 脚本）、剩余打磨 | 发布演练一轮 |

### 验证基线

当前仓库验证手段为：TypeScript 编译、vitest（含架构守卫与生成物校验）、ESLint、cargo test（118 项）。每个阶段完成后四项必须全绿；性能项以"改动前后同规模种子数据下的耗时对比"为准；双平台行为项 Windows 侧依赖阶段二的 CI matrix + 发布后人工回归清单。

---

## 五、正面观察与已排除项

排查中确认**不需要**改的方面，避免后续误伤：

1. **领域层真实落地**：`convertFileSrc` 全仓仅 `domain/mediaUrl.ts` 一处、扩展名硬编码零残留、media_kind 零直读——architectureGuard 的 15 条规则在 CI 真实生效；
2. **生成链路是全项目工程化最好的部分**：单一配置 + 严格校验 + `--check` 模式 + 生成物入库 + CI 新鲜度校验；
3. **生产路径错误处理纪律好**：db.rs 429 个 unwrap 中 428 个在测试模块；"锁内不做文件 IO"有明确注释与自觉遵守；
4. **若干高质量实现可作范式**：OLE 拖出看门狗、win_hook 的 catch_unwind + 重装、media_server 的 Range 支持及单测、径向菜单 store 的选择器订阅、`import_image_file` 的缩略图生成路径（P8 建议向它对齐）、`mediaUrl.ts` 的失败可重试 Promise（B8 建议向它对齐）；
5. **i18n 完整性无缺口**：脚本比对全部静态 key 零缺失，zh/en 差异仅为复数形式；
6. **gitignore 覆盖良好**：node_modules / dist / target / gen/schemas 均未误入库；AGENTS.md 确认未被跟踪（符合本仓约定）；
7. **i18next 与 react-i18next 版本配对正确**（满足官方 peer 范围），不构成问题。

## 附录：证据核实情况说明

- **逐条人工复核过原文的发现**（引用行号与代码摘录可靠）：B1、B2、B9、B10（PRAGMA 部分）、P1、P2（stat 部分）、P3、P5、P6（RadialImageBanner 部分）、P7、A4、A5、A6、E1、E2、E3、E4（build.yml 步骤清单）、E6、E7、E8（tauri.conf 部分）、F1（logo / LICENSE / bundle.resources）、F2、F5、A1（命令计数）。
- **依据专项排查代理引用的行号与代码摘录、未逐条复核的发现**：B3-B8 其余细节、P4、P8、A2、A3、A7-A9、E5（断言计数）、E9、E10、F3、F4、F6、B10 其余子项。这些发现的代码摘录均由排查代理从源文件直接摘取，且同类证据抽查命中率为 100%，可信度高；实施各条时建议先按文中行号定位确认再动手。
- **无法本地核实的事项**：GitHub 仓库的分支保护 / required checks 配置；tauri-action 在 Windows runner 上忽略 `bundle.targets` 的内部行为（"配置与产物脱节"这一事实本身基于 Releases 实况反推，成立）。
